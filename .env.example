# Neon pooled connection string. sslmode=require is mandatory.
DATABASE_URL=postgresql://USER:PASSWORD@HOST.neon.tech/iplegends?sslmode=require

# Where downloaded archives land. Gitignored.
DATA_DIR=./data
