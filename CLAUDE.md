# Working notes

Read `SPEC.md` first. This file tracks state, commands, and open questions.

**`SPEC.md` is the source of truth and this is not a formality.** Four figures recorded
there as measured fact turned out wrong once code actually read the archive: the reduced-
match split (15/19, really 15/12/7), the Impact Player replacement count (523, really 524
deliveries carrying 563 entries), the state-model fitting size (~145,000, really
149,697), and Kohli's 2016 balls faced (637, really 640 — see A22). Each was corrected in
the spec the moment it was found, with the old value named so the correction is auditable
rather than silent. Keep doing that — a figure quietly replaced is indistinguishable from a
figure that was never checked.

The fourth is the worst of the four and worth studying. It was reported here as *verified
against the public record* when only the runs had actually been checked; the balls figure
was asserted to match and did not. Every internal check passed, because the error was a
misreading of cricket's own scoring convention and the database was perfectly consistent
with it. **Only check 7 — a human reading a scorecard — could catch it.** When claiming a
figure matches an external source, check the figure being claimed, not a neighbouring one.

## Environment

Python 3.11 pinned via `uv`. No Homebrew, no Docker, no local Postgres. Database is
Neon (managed), so no Postgres extension may be used.

`.env` needs **both** Neon endpoints. `DATABASE_URL` is pooled (application);
`DIRECT_URL` is unpooled and is what migrations and every bulk load use, because
PgBouncer transaction mode breaks DDL and `COPY FROM STDIN`.

```bash
export PATH="$HOME/.local/bin:$PATH"
uv sync                                   # install deps
cp .env.example .env                      # then fill in BOTH endpoints
uv run python -m etl.download             # resolve + fetch archives, write manifest
uv run python -m etl.inspect_teams        # deck shape + archive audit
uv run python -m etl.migrate --status     # show applied vs pending
uv run python -m etl.migrate              # apply pending migrations
uv run python -m etl.load                 # load season 2016 (the default)
uv run python -m etl.load --season 2008 --season 2020
uv run python -m etl.load --all           # the whole archive
```

Reloading a season is safe. `etl.load` deletes the target matches first and
`deliveries`/`appearances` cascade from them, so a reload replaces rather than
duplicates. `people` is upserted, since a person spans seasons.

Parse-check migrations without a server (catches syntax errors before they hit Neon):

```bash
uv run --with pglast python -c "
import pglast, pathlib
for p in sorted(pathlib.Path('migrations').glob('*.sql')):
    pglast.parse_sql(p.read_text()); print('OK', p.name)"
```

## Hard rules

- **Bulk load with `COPY FROM STDIN`, never row-by-row inserts.** This is the only reason
  a remote database is viable for `deliveries`.
- Never key a player on a name string. Always `person_id` from `info.registry.people`.
- Never use `info.season` as the season year. Derive from match dates.
- Never fabricate a value. NULL, log it, report it.
- **Where the source carries a field we can derive independently, derive it and validate
  against the source rather than storing both.** [A19] Two copies of the same fact drift,
  and the storage is the lesser cost — what is really lost is the check. Deriving turns a
  redundant column into a test of our reading against the source's own answer, which is
  the only kind of check that cannot pass by agreeing with itself. `actual_delivery` is
  the worked example: not stored, and validation 15 is stronger for it.
- **Balls faced is `extra_wides = 0`, not `legal_ball`.** [A22] A no-ball is a ball faced;
  a wide is not. `legal_ball` is for overs bowled and the bowling economy denominator only.
  The two denominators are different predicates and sharing them inflates batting ratings.
- **A null must propagate as unknown.** Never let it acquire a default downstream. Prefer
  filters that test for the known value (`scheduled_balls = 120`) over filters that test
  for the absence of a problem (`not was_reduced`), because a null fails the first by
  construction and slips through the second.
- **Never default an unobserved value to the majority class, anywhere a gap in the source
  can masquerade as a real value.** [A23] This is the general form of the rule above and
  it applies to every derived field, not just the one that broke. A majority default is
  the single hardest error for internal checks to catch: it is plausible, it is right most
  of the time, and where the source is blind it is wrong silently and in a body rather
  than one row at a time. §6.1 defaulted unobserved nationality to Indian on the perfectly
  sound reasoning that uncapped IPL players are overwhelmingly domestic — and the archives
  turn out to contain no Afghanistan at all, so the rule minted eight confidently false
  records including an `is_overseas = false` that would have let a legal XI field five
  overseas players while the check reported compliance. **If a field can be unobserved, it
  must be able to be NULL.**
- **When a rule and the schema that serves it are decided together, verify the schema
  carries every input the rule names — before applying the migration.** This is a standing
  check, not a note about one bug. Migration 007 stored `runs_remaining_total`, and the
  *same message* corrected SPEC 7.1 to price a wicket by expected **final** total, which is
  `runs_so_far + runs_remaining`. Both halves were reasoned about carefully and separately;
  nobody put them side by side, so a rule went into the spec that the table just applied
  could not compute. Migration 008 added the column. **The fix was two lines and is not the
  lesson.** The reconciliation step is the one that got skipped, and it gets skipped
  precisely when both halves feel already-decided. Expect this to recur in the simulator,
  where the parameters it consumes and the tables that hold them will be specified together.
  **It recurred immediately.** Migration 009's `not_rateable_reason` CHECK and the loader's
  gate were specified in the same pass, and reconciling them found that "matches played" had
  two candidate denominators disagreeing on 890 of 3,333 player-seasons and flipping 61
  across the gate. This check is not a formality even one section after it was written down.
- **A rule enforced by a CHECK is protected by the database. A rule that lives in loader
  behaviour is protected by nothing unless a test points at it, so a ratified loader rule
  without a test is an unprotected rule.** Schema rules announce themselves — a reader of
  the migrations sees them. Behavioural rules are invisible: A37's shared state-resolution
  walk has no column and no constraint, so nothing tells a future reader it was ever
  decided, and the day somebody rewrites the walk it vanishes silently with a moved rating
  as the only symptom. When ratifying a rule, ask which of the two kinds it is; if it is
  the second, the test ships **with** the rule, not after it. `tests/test_impact.py` is the
  worked example, and every assertion in it was verified to fail with the corresponding line
  broken — an unfalsified test is the same unprotected rule wearing a badge.
- No LLM or AI API call anywhere in this codebase.
- **A migration is immutable the moment it has been applied to any database, including a
  local or personal one. Until then it is a draft and may be edited freely.** The runner
  checksums applied files and refuses drift. `003` was edited in place on 2026-07-29
  (`innings_scheduled_overs` -> `innings_scheduled_balls`) under a one-time exception,
  granted only because no database existed yet. This is not a precedent. The rule was then
  honoured for real: A22 found a wrong column comment in the applied `003` and it was
  corrected in a new `005` rather than edited in place.

## Deck shape (measured from the archive, not estimated)

Regenerate with `uv run python -m etl.inspect_teams`; also persisted to
`data/manifest.json` under `ipl_archive_audit`.

| | |
|---|---|
| Seasons | **19** (2008–2026) |
| Matches | **1,243** |
| Team-name strings | 19 |
| Franchises | **15** |
| **Franchise-seasons (deck size)** | **166** |

Every deck decision depends on the 166. Franchises by season count: Delhi, Kolkata,
Mumbai, Punjab and Bengaluru 19 each; Chennai and Rajasthan 17 (2016–17 suspension);
Hyderabad 14; Deccan, Gujarat Titans and Lucknow 5; Pune Warriors 3; Gujarat Lions and
Rising Pune 2; **Kochi Tuskers Kerala 1**.

Deck sampling is **uniform over franchise-seasons**, so Mumbai appears 19x and Kochi 1x.
Intended — rarity should feel like an event. Must be a named constant in the dealer.
Never weight by franchise.

## Archive anomalies (permanent notes)

**Season labels.** `info.season` is unusable. Three observed anomalies: 2008 says
`2007/08`, **2010 says `2009/10`**, 2020 says `2020/21`. The 2010 case was not in the
original brief. The trap is the asymmetry — 2009, played in South Africa where a split
label would be forgivable, is labelled plainly `2009`, while 2010 is not. No pattern to
exploit; always derive from match dates.

**Missing matches.** Abandoned-without-a-ball matches are absent from the archive
entirely, because no deliveries means no file. Ten interior gaps across seven seasons:

| Season | Missing match numbers |
|---|---|
| 2008 | 47 |
| 2009 | 7, 13 |
| 2011 | 20 |
| 2012 | 32, 34 |
| 2015 | 25 |
| 2017 | 29 |
| 2024 | 63, 66 (**and 70**, see below) |

Proved by contrast, not assumed: 2011 #68 *is* present carrying `"result": "no result"`,
because play did occur before abandonment. So absence means zero balls, not missing data.

**The detector only sees interior gaps.** A match missing from the tail of a schedule is
invisible, because the highest number observable is the highest one present. 2024 is
short three league matches but only #63 and #66 are detectable; #70 cannot be inferred
without knowing the intended schedule size, which is not in the data. The audit therefore
also records `highest_number_seen` and `numbered_matches_present` so the shortfall is at
least visible. Do not add a hardcoded expected-schedule table to close this — it would
rot, and the consequence is nil since a zero-ball match contributes no rows anyway.

Recorded per season in `data/manifest.json` under `ipl_archive_audit.match_numbering`,
keyed to the archive checksum, so a future gap can be told apart from a download failure.

**`info.overs` never varies.** It is `20` in all 1,243 matches. The brief's §4.5 premise
that it drops for rain-reduced games is **false for this archive**. Reduction is visible
only via:

- `innings.target.overs` — the *chasing* innings' revised length (34 matches have < 20)
- innings 1 ending before 20 overs with fewer than 10 wickets down — curtailed mid-innings

**[Corrected 2026-07-30 — the earlier 15/19 split was wrong.]** Of the 34 reduced matches,
**15** had innings 1 equal to `target.overs`, **12** had it at full length with only the
chase cut, and **7** had innings 1 curtailed at a third length written down nowhere in the
file. The rule that resolves this is SPEC §4.5/A17.

The load-bearing part of that rule: when the chase was scheduled for a full twenty, innings
1 was too, and ended on wickets. Without that clause 44 innings go null instead of 6,
because an ordinary batting collapse in the 19th over looks exactly like rain from inside
a single innings. Six innings remain genuinely unknowable and are logged by name:
`1136566`, `1136592`, `980989` (shortened, abandoned mid-over) and `1473495`, `1527685`,
`501265` (abandoned with no second innings).

Count **overs, not legal balls**, when reading innings-1 length — otherwise the two 5-ball
miscounted overs below read as 119-ball curtailments.

**`target.overs` can be fractional.** Match `392186.json` carries `9.2` — nine overs and
two balls. Any integer overs column cannot represent it; scheduled length must be stored
in **balls**.

**`miscounted_overs`.** Cricsheet flags eight overs across the archive where the umpire
miscounted, giving 5 or 7 legal balls instead of 6. Validation check 3 must whitelist
these rather than treat them as parser bugs — they are real, and the source declares them:

`1136564` inn2 ov11 (7) · `335987` inn1 ov7 (5) · `335994` inn2 ov10 (7) ·
`336015` inn2 ov14 (5) · `392198` inn2 ov10 (7) · `419155` inn1 ov18 (7) ·
`501202` inn1 ov5 (5) · `501255` inn2 ov9 (5)

**`replacements`** appears on 585 deliveries in two kinds: `match` is the Impact Player,
`role` is a substitute taking over a fielding or bowling role. Both feed the A4
batting-position rule. **[Counts corrected 2026-07-30]** `match` is on **524 deliveries**
carrying **563 entries** — some deliveries record two at once — not 523. `role` is 61.
`match` entries always name a `team`; `role` entries never do and never need to.

**`actual_delivery`** is on every delivery and is the *legal-ball* scorecard reference, so
it repeats after a wide (11,075 duplicates). Not stored — verified exactly derivable from
`legal_ball` on all 295,732 deliveries, which makes it a free check on our own wide and
no-ball classification against the source's answer. Validation check 15.

**Powerplay bounds are positional, not legal-ball.** `powerplays[].to` reaches `5.9`,
which no legal-ball index can produce. Compare against `ball_no`.

**Super overs.** 16 matches, of which `1216517` (Mumbai v Kings XI, 2020) went to a
*second* super over and holds six innings. Ties and eliminators coincide exactly: all 16
ties carry `outcome.eliminator`, nothing else does, and a tie carries no `winner` key.

**At most one wicket per delivery** across all 295,732 — the single `wicket_kind` /
`player_out_id` pair on `deliveries` is safe and does not need a child table.

**`outcome.method`.** Present on 23 matches, always `'D/L'`, never anything else. All 23
sit inside the 34 reduced matches, so D/L is a strict subset of reduction and not a
separate exclusion criterion. But a D/L result carries an ordinary margin — 13 by runs,
10 by wickets — so it is invisible in `result_type` and would have gone unrecorded without
`matches.decided_by`.

**2026 revision risk.** Cricsheet is contributor-driven and recent seasons get revised.
Re-verify the 2026 files against a fresh download before finalising ratings. If the 2026
baseline sits far off trend after within-season normalisation, flag it rather than
absorbing it silently.

**Reported verified by the user on 2026-07-31.** Recorded here with the claim named, per
A22: what this discharges is the 2026 season data, and **the figure the record now rests on
is V Suryavanshi's 776 runs off 327 balls (strike rate 237.3)**, which is the largest single
outlier in nineteen seasons and carries a 95 rating. The archive hash is unchanged from A9
(`841b9829...`, 5,180,977 bytes), so this is a verification of the same bytes A9 closed on,
not of a re-download. **If that specific line was not what was checked, this is not
discharged** — A22 is exactly the case where a neighbouring figure was checked and the
claimed one was not. Independently, 2026's season mean sits at 78.4 against a 78.0-78.9
range across all nineteen seasons, so the baseline is on trend rather than off it.

## Storage budget (Neon free tier)

Free tier is **0.5 GB logical data per project**; instant-restore history is metered
separately with a 1 GB-month allowance, so reload churn during development does not eat
the 0.5 GB.

| | |
|---|---|
| Matches (2008–2026), measured | **1,243** |
| Deliveries per match | ~250 (240 legal + extras) |
| **Total delivery rows** | **~310k** |
| `deliveries` heap | ~40 MB |
| 5 indexes on `deliveries` | ~50 MB |
| Everything else | <10 MB |
| **Total, estimated** | ~100 MB against a 500 MB cap |
| **Total, MEASURED after the full load** | **70 MB** (deliveries 65 MB, appearances 3.8 MB, rest <1 MB) |
| **Total, MEASURED after SPEC 6 and migration 006** | **69 MB** (deliveries 63 MB after the phase index went, appearances 3.9 MB, squad_members 520 kB) |
| **Total, MEASURED after SPEC 7.1 and migration 007** | **68.6 MB** — the state model costs **88 kB** for 241 rows. The fitted grid is four orders of magnitude smaller than the deliveries it was fitted on, which is the whole argument for storing it. |
| **Total, MEASURED after SPEC 7.2-7.3 and migration 009** | **73.1 MB** — `player_season_impact` is **1,080 kB** for 5,149 rows, most of it the two indexes. Every rating in the game costs one megabyte. |

Roughly 7x headroom, so the estimate was conservative rather than wrong. The brief's "1.2 million deliveries" figure is ~4x high. The exact
count, from parsing the whole archive, is **295,732 deliveries across 1,243 matches and
28,106 appearances** — not an estimate.

## Decisions log

| # | Decision |
|---|---|
| A1 | Extras split into 5 smallint columns. Byes/legbyes are not charged to the bowler, so one column makes economy wrong. |
| A2 | Cohort offsets pooled across seasons, not per-cell z-scoring. Validation 10/11 replaced with predictive under/over-shrinkage tests; new check 13 for cohort era-drift. |
| A3 | Nationality from Test + ODI + T20I archives combined. Unknown list gets full review, sorted by matches played desc. **Superseded in part by A23 — the India default is gone.** |
| A4 | Batting position scans both `batter` and `non_striker`. Retired hurt/out, concussion subs and Impact Player entrants keep their original position. |
| A5 | Display phase: death = final 25% of *that innings'* scheduled overs. State model is separate: exact over x wickets bucketed 0-1/2-3/4-5/6+, fitted on full-length innings only, reduced innings mapped by balls remaining. |
| A6 | Slot template revised. Keeper is orthogonal to batting band. Bands from 6.4 are canonical. |
| A7 | Career shrinkage prior computed leave-one-season-out. `appearances` splits `named_in_squad` from derived `participated`. |
| A8 | Pace/spin slots collapse to a generic bowler slot of 5 until `bowling_style.csv` is filled. That CSV ships right after the full archive load. |
| A9 | Scope is 2008-2026, 19 seasons. **CLOSED 2026-07-30, both conditions discharged.** The archive was re-downloaded from cricsheet.org and is bit-identical to the one every figure here was measured from (`841b9829...`, 5,180,977 bytes) — **re-fetched, not re-hashed off disk**, which would only have proved the local file was untouched. And 2026's baseline is on trend rather than off it: batting +0.1869 (2024) -> +0.2149 (2025) -> +0.2567 (2026), the last step the same size as the one before. A later re-download that differs is a reload and a refit, not a discrepancy — the hash is what tells those apart. |
| A10 | Deck sampling uniform over the 166 franchise-seasons, as an explicit named constant. Never weight by franchise. |
| A11 | Franchise-reroll availability queried from data at deal time, never hardcoded. |
| A12 | Season-label and missing-match anomalies recorded permanently; gaps persisted to the manifest keyed to the archive checksum. |
| A13 | `info.overs` is a constant carrying no information. Scheduled length comes from `target.overs` and innings-1 truncation, stored in BALLS (9.2 overs -> 56). |
| A14 | Miscounted overs taken from the source flag, whitelisted in validation, dropped from the 7.1 fit. |
| A15 | State table fitted on FIRST innings only, reduced matches and miscounted overs excluded. Reduced-match deliveries excluded from rating but kept in display stats. Second-innings table built only if validation 14 fires. |
| A16 | `replacements` kinds: `match` = Impact Player, `role` = substitute. A substitute who bowled counts as participation; a named-but-unused Impact Player does not. |
| A17 | Innings-1 scheduled length resolved by a six-case rule needing match-level context, not innings-level. Null for exactly 6 innings, each logged. Corrects A13's 15/19 split to 15/12/7. |
| A18 | A tie is stored as `result_type = 'tie'` with `winner_fs_id` set to the eliminator. Match won, match tied, both recorded. One match holds two super overs (six innings). |
| A19 | `actual_delivery` not stored — exactly derivable, so it serves as an independent check on wide/no-ball classification instead (validation 15). Powerplay bounds are positional, compare against `ball_no`. Generalised into a hard rule above. |
| A20 | `matches.decided_by` (runs / wickets / eliminator / dls / null) makes the result row self-describing. Takes `dls` over the margin type, since the margin type is already in `result_type` and D/L is recorded nowhere else. |
| A21 | §7.1 fitting filter tests `innings_scheduled_balls = 120`, not `not was_reduced`. The latter leaked 148 deliveries from 3 matches abandoned in innings 1 with no chase to mark them reduced. |
| A22 | Balls faced = `extra_wides = 0`. A no-ball is a ball faced, a wide is not, so `legal_ball` is the wrong predicate and undercounts. Corrects Kohli 2016 from 637 balls to 640. No new column (A19); migration `005` fixes the wrong comment in the immutable `003`. Pinned by validation check 17. |
| A23 | **Corrects A3.** Unknown nationality is NULL, never a default to India. The three international archives contain **no Afghanistan team at all**, so the default was systematically wrong for a whole nation — 8 Afghan internationals were being written down as Indian and not overseas. `is_overseas` is NULL too, since it was a claim derived from a value never observed. Composite sides (ICC World XI, World XI, Africa XI, Asia XI) excluded from the vote: Rashid Khan resolved to `ICC World XI` on one cap. 502 resolved, 314 unknown. |
| A24 | Keeper role is per franchise-season, from stumpings attributed to the season they happened in — not the career `people.is_keeper` flag, which gave exactly one keeper to only 41 of 166 franchise-seasons. Now 116 with one, 24 with two, 26 unproved. `keepers_by_season.csv` carries the per-squad answer; blank is undecided, not yes. |
| A25 | Two phases level on the same count is `mixed`, not whichever the tie-break picks. A tie for the lead among three phases can only be 50/50, which passes the >= 50% dominance test, so the naive rule names a phase by coin toss. |
| A26 | Role thresholds asymmetric and calibrated, not chosen: `BAT_MIN = 9.0` balls faced/match, `BOWL_MIN = 6.0` legal balls bowled/match. Symmetric cutoffs gave 2.9% or 12.6% all-rounders with a "neither" bucket. Falsifiable window pinned by a 12-player calibration table in `etl/roles.py`. **Provisional — see the note below the table.** |
| A27 | A player who neither batted nor bowled gets no `squad_members` row. `role` is NOT NULL and fielding a dismissal implies no role. 47 of 3,384 pairs excluded, 3,337 written. |
| A28 | §7.1 fitting filter is `innings_scheduled_balls = 120` alone; the `not was_reduced` clause is dropped. It excluded 12 first innings that were themselves scheduled for and played to a full twenty — the reduction fell on the chase, after they ended. 149,697 -> **151,175** deliveries. Refines A21, which remains correct about *why* `= 120` is the right predicate. |
| A29 | `deliveries_phase_idx` dropped in migration `006`. Three distinct values over 295,732 rows: the planner will not choose it, and every query that filters on phase also filters on match or franchise-season. `deliveries` is 63 MB, down from 65. |
| A30 | **The four override CSVs cannot be narrowed further by derivation, and that was measured rather than assumed.** Afghanistan's absence is documented Cricsheet *policy* — 158 matches withheld — so no re-download will ever close it. No nationality-restricted competition exists among the 1,115 published archives. Catches name the proven keeper 51% of the time (86% within the top 3), and the career-keeper cross-reference is **wrong for 5 of the 39 squads it is confident about**, failing in the A23 shape. So the generators emit **evidence and a ranking, never a verdict**: `merge_override` now takes the decision column by name, preserves it untouched, and refreshes every other column. `cricinfo_id` added to all four files. No migration was needed — A19 stands unchanged. |
| A31 | **§7.1 state model stored as two tables, migration 007, because the grains genuinely differ.** `state_ball_outcomes` is (over x bucketed wickets), 80 rows; `state_runs_remaining` is (over x **exact** wickets), 161 rows. One grain cannot serve both: per-ball rates are too noisy for exact wickets, and inside a two-wide bucket one more wicket usually stays in the bucket, so a bucketed runs-remaining grid would price a wicket at zero for half the states. **Counts only, never rates** (A19), with a CHECK that the seven outcome columns sum to balls faced. **The cost of a wicket is the drop in expected FINAL total, not the difference in expected runs remaining** — the latter is confounded by runs already scored and comes out **negative** at over 12, 0 down. SPEC 7.1 corrects the old wording and names it. All 80 states stored, the 5 never observed as **explicit zeroes**, because an absent row and a zero row say different things to the simulator. Thin cells kept raw with their `n` and shrunk by the consumer, never smoothed at rest. |
| A32 | **Shrinkage cannot carry the low-volume problem, and §7.2 was written as though it could.** Shrinking toward an own-career prior protects against a thin *career* and never against a thin *season*: for a thin season the prior is mostly that same thin season, so a larger `k` makes the outlier climb rather than fall. Measured: BA Bhatt's 5-ball 2011 goes **+2.003 -> +2.206** as `k` runs 50 -> 800. No value of `k` fixes this. The fix is a gate, not a constant — see A33. |
| A33 | **Two draftability gates, not one.** The §7.3 four-match gate stays, and a **balls floor** joins it: **100 balls faced** for batting, **150 legal balls bowled** for bowling. Below the floor a player-season is *not rated at all* — not shrunk, not floored, simply not draftable from that squad. Floors measured from where the raw top-5 stabilises, per discipline rather than assumed to transfer: batting settles at 100 (+0.99 at 100, +0.97 at 150 and 200), bowling only at **150** (+0.87 at 100 with RG Sharma's 138-ball 2009 in the top twelve; +0.76 at 150 and 200, and he is gone). 150 balls is ~7 matches, so the match gate is non-binding for bowling. Rateable: **1,027 of 2,954** batting seasons, **787 of 2,195** bowling. **Both floors are empirically derived from where each raw list stops moving, not round numbers** — they only look round because of the sweep grid, so a revised archive moves them rather than keeping them. **Both gates count the scoring set**, not `squad_members.matches_played`: the two disagree on 890 of 3,333 seasons by up to 7 matches and flip 61 across the gate, and a gate asking "is there enough evidence to rate this?" must count the evidence the rating was computed from. Confirmed after the load — all 61 newly-gated seasons are marginal (max 63 balls faced, max 72 bowled) and **0 of 61 would have cleared a balls floor anyway**, so the tightening is right in principle and inert in effect. |
| A34 | **A residual survives the floor and is recorded rather than papered over.** Suryavanshi 2025 (122 balls, raw +0.574) shrinks **up** to +0.754 and outranks Sehwag 2011 (240 balls, raw +0.615, shrunk +0.429). Strictly-dominated inversions — fewer balls *and* lower raw, yet higher rating — are **4.56% within-band**. Dropping A7's own-career LOSO prior for a band-season-mean prior alone gives **3.71%** and raises Kohli's retained spread 68% -> 80%, at the cost of A7. **Explicitly provisional and deliberately not fixed now** — the repair means calibrating a new prior against pre-normalisation numbers, and §7.4 changes exactly the baseline the prior is measured against, so tuning it twice risks landing on a value that was right for the wrong baseline. **A34 and A35 are one decision viewed twice** (how hard to pull a thin season toward a prior, and which prior) and **resolve together in a single pass post-§7.4**. Neither may be ratified alone. **RESOLVED by A46 — the band prior won and A7 is reversed. The 4.56% / 3.71% figures here are pre-normalisation and are NOT comparable in level to A46's; only the ordering reproduced.** |
| A35 | **`k = 100` is provisional and is not settled until re-checked post-§7.4 against within-season-normalised numbers.** Chosen for noise protection over spread: Kohli's within-player spread retained runs 81/68/53/38/23% at k = 50/100/200/400/800. Normalisation may make a lower `k` affordable. **Do not treat 100 as ratified.** Kohli calibrates within-player spread and ordering only, never level — the league mean is not zero and drifts from **-0.188 (2009) to +0.205 (2026)**, which is exactly what §7.4 removes. **Resolves in the same pass as A34.** `k` lives in exactly one place, the `player_season_rating` view, and is never a stored column — a stored shrunk value plus a stored `k` are two copies of one fact and would drift the moment the constant is revisited, which is guaranteed. Re-tuning is a view replacement and no reload. One view, single source, no exceptions. **RESOLVED by A46: k = 100 survives re-checking and is now ratified. The single-place rule is unchanged and permanent.** |
| A36 | **Dismissal attribution is striker-only.** Charging a batter for a non-striker run-out is wrong on its face. 7,458 fitting-set dismissals, **319 (4.28%)** belong to somebody other than the striker. The aggregate effect is tiny (**+0.0010**/ball mean) and the per-player effect is not (**max +0.193**, min -0.049, two changes in the top 20), which is why the aggregate could not have settled it. The any-wicket grid is kept as a **labelled diagnostic only**, never consumed by ratings. Bowling attribution is `credited_to_bowler`, so run outs are excluded there too. |
| A37 | **One state-resolution rule across both halves of a ball's impact.** The runs baseline silently `continue`d on thin cells while the wicket cost fell back to the nearest state — so **867 balls (0.31%)** were dropped, and non-randomly: thin cells are collapse and death states, so the drop biased against exactly the players who batted in them. Kohli 2016 read **838 off 583** against a real **860 off 590**. Both baselines now take the same nearest-bucket-in-the-same-over walk. After the fix: **19 of 19** Kohli seasons reconcile exactly, 2016 = **860 off 590**, and nothing is dropped — 886 of 281,389 batting balls and 921 of 280,192 bowling balls resolve to a neighbour instead. `uv run python -m etl.impact --reconcile` is the standing check. **A37 has no schema representation and never will** — it is a property of a walk, so `tests/test_impact.py` is the only thing protecting it, per the standing rule above. |
| A38 | **`player_season_impact` is long grain, migration 009 applied 2026-07-30.** One row per (franchise-season, person, **discipline**), not one wide row with batting and bowling side by side. Measured: 5,149 long rows against 3,333 wide, and the wide form carries a NULL half on 2,132 of them because most player-seasons are one discipline only — and a discipline that does not exist reads identically to one that scored zero. Long grain also lets A33's two different floors share one column. **Counts and inputs only, never a rating** (A19): `impact_total` is the SUM, and the gate constants are stored beside the result so the `not_rateable_reason` CHECK can recompute the reason and refuse a loader that gates on one rule and writes another. Reasons after the load: `balls` 2,086, NULL 1,812, `both` 1,249, `matches` **2** — Symonds 2008 (105 balls, 3 matches) and Hussey 2008 (100 balls, 3 matches), both verified to have identical all-match and scoring-set counts, so the match gate earns its keep independently of the A33 denominator choice. |
| A39 | **A33's floors leave whole cohorts unrateable, and this is a §7.4 and check-12 problem recorded before §7.4 rather than discovered inside it.** Coverage of the 166 franchise-seasons, counting how many can field a *rated* player per slot: opener 166, top_order 165, middle 150, **finisher 43**, **tail 0**; bowling middle 164, mixed 148, powerplay 137, **death 8** (of the 66 that have a death bowler at all, and 3 of the 8 are DJ Bravo). **`tail` is not thin, it is empty** — 0 of 707 tail player-seasons clear the 100-ball batting floor and the largest tail season in nineteen years is **95 balls**, so no tail batter can ever be rated on batting. That is the floor being *correct* — a number 9 genuinely has no measurable batting season — but it has three consequences. (1) §7.4 cannot estimate a cohort offset for `tail` from zero observations and must not try; `death` at 8 is the same problem in A2's stated form. (2) **Check 12 must be written to FAIL on slot coverage, not tolerate it.** (3) The deck may be fine anyway, because a tail batter is drafted for bowling and **393 of 730 tail player-seasons are bowling-rateable** — but that is a claim about the slot template (A6/§6.4) that has not been checked and must be, before §7.4 assumes it. **Do not resolve any of this by lowering a floor** — A33 measured both and they are where the lists stop moving. |
| A40 | **The slot template was provisional pending feasibility; it is now measured, repaired and CLOSED.** `etl.feasibility` plays the draft out — 15 picks, uniform deck, dedupe on `person_id`, the §1.1 deal-time guarantee — instead of counting slots. **The completion rate was the wrong headline: with the guarantee on the draft completed 100% of the time under every policy, and guarantee-off the same template failed 1.7% / 55.5% / 54.4% for a rational / naive / random drafter**, stranding on **`finisher`** then `keeper`. SPEC called the guarantee a safety net and it was a load-bearing beam — and a guarantee that fires constantly is quietly collapsing the variety A10's sampling exists to create. **The repair, ratified: `middle` (x2) and `finisher` (x1) become one `middle_or_finisher` band of three covering positions 5-8**, because `finisher` alone was servable from 43 of 166 franchise-seasons and **29 distinct people in nineteen years**, and because the 5-to-8 order is a cricketing continuum rather than two jobs. **`keeper` is resolved by the hand-filled `keepers_by_season.csv`, not by a template change** (126 of 166, 35 people, *with the CSV unfilled*). Measured on the real merged template, not the simulated variant: supply **154 fs / 117 people** (was 150/102 + 43/29), rational guarantee-off **0.0%, 0 of 2,000**, naive 10.7%, random 16.2%, and **the guarantee fires on 0.0% of rational drafts** — it is a net again. **The bottleneck did not move up one position, which was checked rather than assumed**: `opener` (166 fs / 108 people) and `top_order` (165 / 134) are numerically identical pre- and post-merge, so the merge added supply and took none. Thinnest slot is now `keeper`, i.e. check 19's business. **The guarantee stays under two new conditions: it must log every firing** (the report prints drafts-fired, picks-fired, worst-single-pick) **and the free-pass fallback stays.** |
| A41 | **SPEC 7.4's stated reason for cohort offsets was wrong, and the offsets are kept anyway with the reason rewritten.** 7.4 justified them by saying a great finisher would "be compressed to 61 for facing fewer balls than an opener" — that describes RAW STRIKE RATES, and impact is scored per ball against the exact (over, wickets) state, so **the state model has already made the situational correction before cohorts are reached**. Measured after centring: batting opener -0.016, top_order -0.008, middle +0.033, finisher **+0.019** — finishers score ABOVE average per ball, the opposite of the premise. Whole cohort spread is **0.049** against a within-cohort SD of 0.185 and a season drift of **0.25**, five times larger. Offsets kept because they are cheap and still move 27 ranks of 1,025, but as a **small residual**. **The old rationale is deleted rather than softened** — a reader acting on it would over-correct by roughly a factor of five. |
| A42 | **Centre within season; do NOT divide by the season SD.** The level drift is real and large (batting +0.007 in 2008 to +0.257 in 2026, bowling mirroring it +0.083 to -0.111) — without centring nobody would ever draft a 2009 squad. The SPREAD drift is not real: season SDs range 0.136-0.216 but **only 1 of 19 seasons per discipline sits more than two sampling SEs from the pooled SD**, so it is estimation noise off ~50 observations, and dividing by it **reorders 564 of 1,025 batting seasons by more than ten places on nothing at all**. Same argument as A2 one level up: do not normalise by a quantity you cannot estimate stably. The mean is **UNWEIGHTED** — one entry per player-season, never per ball — because the rating is a statement about players, so the reference population is players; ball-weighting would let high-volume players define the baseline everyone else is measured against. The two differ by ~0.03. |
| A43 | **A cohort below 20 observations gets a ZERO offset, and that is zero-by-insufficient-evidence, not zero-by-measurement.** `death` has 8 rateable seasons at +0.002 +/- 0.125 — a coin flip near zero, and fitting it is fitting noise. `tail` has none at all (A39). Expressed as a **threshold, not a hardcoded list of two cohort names**, because a name list goes stale silently: a revised archive giving `death` forty seasons would leave the hardcoded zero standing and wrong. The threshold sits in a wide empty gap — `death` 8, next smallest `finisher` 45 — so any value in (8, 45] behaves identically today and the exact number is not load-bearing. Same shape as A33's floors: a rule about evidence. |
| A44 | **The 0-100 display scale is LINEAR on the normalised value, globally anchored, with a guard clip at S = 3.5.** `rating = 50 + 50 * clip(normalised / (3.5 * SD), -1, +1)`. Percentile is more legible one number at a time and **destroys the gaps, which are the only reason the ratings are worth building** — Bumrah 2020 standing clear of the field is real information, and under percentile two players a hair apart and two a chasm apart show the same gap, so a drafter cannot see which pick is the real edge. **One SD for the whole deck**, both disciplines and every season and cohort: batting 0.1838 vs bowling 0.1766 is a 4% difference, so sharing costs almost nothing and buys the comparison a drafter actually makes — a batting 92 and a bowling 92 are the same distance in runs per ball. The clip is a **guard, not a transform**: observed range is -3.12 to +3.60 SD, near symmetric with no fat tail because shrinkage already pulled every extreme season toward its prior, and **at S = 3.5 the clip touches exactly ONE of 1,812 rows**. S = 3.0 would destroy seven gaps to buy eleven points of scale — clipping is percentile's sin applied locally. SD is **derived, not frozen as a literal** (A19), so a revised archive rescales, which is correct: the scale is defined relative to the league it describes. |
| A45 | **`normalised_per_ball` is what the simulator consumes; `display_rating` is the UI number and the simulator must never read it.** The display value is clipped and rescaled for legibility — feeding it to a match engine would push a clipped season's real edge into the engine as a tie. `raw_per_ball` was **removed** from the view for the same class of reason: it is the pre-normalisation number, and leaving it beside a normalised one is offering two ratings and inviting a caller to read the wrong one. Verified unread outside the view before removal. Migration 010 is `drop` + `create`, not `create or replace`, because Postgres only lets a replacement APPEND columns and this one reorders and removes. |
| A46 | **RESOLVES A34 and A35 together. The prior is the cohort-season mean, not the own-career LOSO mean — this REVERSES A7 — and `k` stays 100, now ratified.** Mechanism: shrinking a player toward their own career mean pulls **every one of their seasons toward the same point**, so it erases exactly the within-player contrast the game is about. A7 reasoned a player is their own best comparison, which is true of the **level** and exactly wrong for the **spread**. Measured on normalised numbers, band beats loso on all three criteria at every k; at k = 100 band gives Kohli spread **63%** retained, thinnest top-five season **249 balls**, dominated inversions **9.87%**, against loso's 56% / **122** / 10.97%. k = 50 (71% / 141) and k = 300 (50% / 104) both **fail the sub-150-ball rule**, so k = 100 is where both constraints hold. Result: Suryavanshi's 122-ball 2025 is out of the top five, thinnest top-five is Russell 2019 at 249 balls, Kohli runs **2016: 75 down to 2022: 30**, a 45.7-point spread. **A34's 4.56% / 3.71% are pre-normalisation and not comparable in level to the figures here — only the band-below-loso ordering reproduced.** Prior source is written to `player_season_impact.prior_source` as `'band'` on all 5,149 rows; **no migration was needed**, 009's CHECK already admitted it. |
| A47 | **A rating becomes a ball by EXPONENTIAL TILTING of the state distribution, not by a hand-written split between runs and wickets.** `q(x) = p(x)e^(θx)/Z` with θ solved so the tilted mean is `E_p[x] + bat − bowl`. The property doing the work is that this is the minimum-KL distribution with that mean: **it adds no information beyond the mean asked for**, so the state's own shape decides how a rating splits between scoring more and being dismissed less. The dismissal sits in the same distribution at `−wicket_cost(state)` (A31's final-total cost), so one θ moves both halves. Three consequences: **θ = 0 is exactly the identity**, which is what makes `--validate` a real test rather than a tautology; **a zero stays zero**, because the tilt multiplies — an additive adjustment would invent a five off the bat in a state that never produced one, and 5 of the 80 states have genuinely zero mass somewhere; and an unreachable target saturates rather than diverging. Solved by bisection, which needs no starting point because the tilted mean is strictly increasing in θ. |
| A48 | **An unrated batter bats at his BAND's pooled below-floor level, not at zero and not at a hastily-shrunk individual number.** A33's floor says there is not enough evidence to rate a tail-ender's batting; it does not say he bats like the league. Zero would mean league-average, which is the A23 failure exactly — an unobserved value defaulting to a plausible majority. A small `k` would manufacture an individual number from the evidence A33 already ruled insufficient. The band mean is A43's rule (pool where the individual evidence is thin) applied on the other side of the floor. Measured pooled raw impact per ball, batting, gated: overall **−0.4056** over 1,929 seasons; **tail −0.6362** (707 seasons, 7,144 balls), opener −0.4846, top_order −0.4337, middle −0.3790, finisher −0.2698. A card with no band at all falls to `tail`. |
| A49 | **The four-overseas rule is enforced on the KNOWN count, on BOTH sides of a repair.** A23 left 314 nationalities NULL, so the engine enforces a lower bound and reports the interval — `legal`, `CANNOT BE CERTIFIED: the true count is between x and y`, or `NO LEGAL XI EXISTS`. The half that is easy to miss is the incoming side: **an overseas player may only be swapped out for a KNOWN domestic one.** Swapping in an unknown lowers the count this code can see without lowering the count that matters. Measured over 400 drafted XIs *with the CSV empty*, strict vs loose: **legal 127 vs 118, uncertain 179 vs 224, no legal XI 94 vs 58** — the strict rule certifies MORE XIs, because the loose one was manufacturing uncertainty by design. **After A51's fill the same 400 read 313 / 0 / 87**, so the uncertainty branch is unexercised today and stays anyway, because a revised archive reintroduces unknowns. **The 87 (94 pre-fill) are not an engine bug: §1.1's template carries no nationality constraint**, so the deck can deal a squad that cannot be fielded. Filed with A40's retune, not patched here, because lowering it means changing the template. |
| A50 | **The attack is capped at five and the count is a consequence, not a preference.** Twenty overs at a maximum of four each is exactly five, so `viable()` requires five bowlers and `attack()` returns five. Capping matters as much as requiring: `choose_bowler` equalises workload, so an XI holding seven bowlers gives all seven three overs — the two worst get a full share and the best gets a quarter of his four, which flattens exactly the rating differences the draft exists to express. Found by reading a scorecard, not by a check. |

| A51 | **The IPL's own four-overseas cap is an EXTERNAL check on SPEC 6.1, and it is validation check 21.** Replaying our nationality data against **2,486 real team sheets** tests it against a rule the archive never states — an XI our data says fielded five overseas players is our data being wrong, because the match was played. It found things nothing internal could. (1) **Three of the hand-filled 314 were impossible**: `VS Malik` and `Shoaib Ahmed` entered as Pakistan but played 2009+, and no Pakistani has played the IPL since 2008; `AG Murtaza` entered as Afghanistan but played 2010-2013, and the first Afghan was 2017. A fourth, `WA Mota` (Australia), sits in **10 of his 12 XIs** as an illegal fifth. All four are now India. (2) **The archive-resolved half answers a different question from the one the draft asks** — which nation a player EVER represented, not the one he held that season. `S Sohal` opened for Punjab from 2008 and later played T20Is for the USA, so the vote marks him overseas for seasons in which he was domestic. **This is A23's shape running the other way**: A23 stopped an unknown defaulting to domestic, this marks a genuine Indian overseas. Safer direction, still wrong. **RATIFIED: nationality is the one held DURING the IPL career, not the career vote.** All 13 were web-verified and filled — eight India, three South Africa (capped by SA while they played), two genuine overseas slots. Two are worth keeping for the reasoning: **Tanmay Mishra was a capped KENYA international and still domestic**, signed by Deccan as a local player on his Indian passport, so the question is not 'has he a cap' but 'did he occupy an overseas slot'; and **Sachin Rana was never a Seychelles player at all** — a registry collision, not an emigration. **Check 21 had a bug of its own that inflated the alarm by a third**: it counted everyone who `participated`, including a fielding substitute who took a catch and was never in the XI. On the named-XI basis the same data read 63, not 99. The Impact Player is still counted deliberately — four overseas in the XI obliges an Indian Impact Player, so four across the named twelve is the right bound. **Check 21 now PASSES: 0 of 2,486**, with 816 people, 489 international, 327 override, 0 unknown, and the game's 400 XIs at **313 / 0 / 87**. Falsified rather than assumed: marking Kohli Australian implicates 82 players. |

| A52 | **A stumping is PROOF of a keeper, not the DEFINITION of one, and treating the two as the same thing is what left 26 squads keeperless.** All 26 uncovered franchise-seasons had the same cause: **zero stumpings all season**. The archive holds 388 stumpings over 166 franchise-seasons, about 2.3 each, so a season with none is ordinary luck rather than missing data — and A24's "blank is undecided" then correctly refused to name anyone. The squads had keepers all along: 2008 Chennai had Dhoni, 2022 Mumbai had Ishan Kishan, 2024 Kolkata had Phil Salt. **Resolved from the public record, which is the only place the answer exists** — Cricsheet has no keeper flag (A30) and no internal signal can close it. **29 rows across the 26 squads**, three of them shared gloves (2008 Chennai Dhoni + Parthiv Patel, 2009 Kolkata van Wyk + Saha, 2025 Hyderabad Klaasen + Kishan), which A24 already permits. Check 19 goes 140 -> **166 of 166** (139 with one keeper, 27 with two) and check 12's thinnest slot goes 126 -> **150 of 166**. **The candidate ranking would have got several of these wrong**, which is A30 measured again rather than restated: 2008 Chennai has Dhoni and Parthiv Patel tied on catches at ranks 1 and 2 and both keepers elsewhere, and 2024 Kolkata puts Salt top with `keeper_elsewhere` reading *no* while Raghuvanshi at rank 5 reads *yes*. Reading `catch_rank` alone inverts that one. |
| A53 | **`etl.derive_squads` had been unrunnable since migration 009 and nothing noticed, because nothing had needed to re-run it.** 009 made `player_season_impact` reference `squad_members`, so the loader's plain `truncate squad_members` fails outright — correctly, since every rating is keyed to a (franchise_season, person) pair that lives in the truncated table. **Fixed by NAMING the dependant in the truncate, not by CASCADE.** Postgres needs both in one statement either way, so the choice is purely about what happens to the *next* dependant: named, it fails loudly here; CASCADE, it is emptied silently. And an empty `player_season_impact` is indistinguishable from a fresh one until something reads it — `player_season_rating` returns nothing, every draft comes up empty, and no error is raised anywhere. The loader now prints the row count it is clearing and the command to restore it. **Refresh order is now `etl.load` -> `derive_people` -> `derive_squads` -> `state_model --write` -> `impact --write`**, and `derive_squads` moved into it rather than sitting beside it. |

| A54 | **Per BALL is right for the engine and wrong for a CARD, and four measurements say so.** A drafter buys a player for a match, not a delivery. (1) Kohli 2016, the aggregate record, rated **75.4** against Russell 2019's **100.0** — per match they are **+11.35** and **+12.13**, an 11% gap displayed as 25 points. (2) Warner 2016 beat Tanvir 2008 per match (+12.61 to +9.46) and lost on the card (81.5 to 84.7). (3) Seasons with 4-6 Player-of-the-Match awards — Marsh 2008, Hussey 2013, Tendulkar 2010, Rohit 2016 — sat in the **60s**. (4) Finishers held 4% of rated seasons and **20% of the top twenty**, because a death ball carries **3.93** runs of variance against a powerplay ball's **2.12** and they face half as many, so per-ball rating read noise as merit. Multiplying by balls per match fixes all four and needs no new correction — finishers fall to **0% of the top 20**, because fewer balls now means less rather than more. Migration 011, a view replacement only. |
| A55 | **Batting and bowling ADD; they are already the same unit.** 010 gave an all-rounder two rows and every consumer took `max`, so Narine 2024 — 488 runs at SR 181 **and** a 69.7 bowling season — was carded at 69.7 with the batting discarded. Both impacts are runs above par, so they need addition, not weighting: nothing here re-decides what a wicket is worth, A31 did that from the archive. **123 of 1,689 player-seasons score in both.** Narine 2024 is now **95**. |
| A56 | **Player of the Match enters the rating, because it is evidence we already stored.** Cricsheet carries it on **1,234 of 1,243** matches and migration 002 has loaded it since day one; the ratings ignored it for nineteen seasons. It is the one **human** judgement in the pipeline. Priced at **12 runs per award per match played** — a conversion, therefore a choice, written once in the view — and deliberately too small to carry a season alone: Marsh's five-award 2008 gains 5.5 runs a match against a merit of 16.8. |
| A57 | **A REPUTATION term, and it is a GAME-DESIGN decision rather than a measurement.** The only parameter in the project not derived from evidence about the thing it scores, recorded plainly so nobody later mistakes it for one. Mechanically it is **A7 reinstated as a SECOND shrinkage stage** — each season blended toward the player's other seasons, leave-one-out, at **REPUTATION = 0.45**. A46's removal of A7 from the *first* stage stands; `player_season_impact` still shrinks toward the band prior. This is a different claim applied after the estimate is complete: a card is "Bumrah, **as of** 2026", not "Bumrah **in** 2026". His range becomes **78-98** rather than 37.6-92.9. Two guards, both load-bearing: the career estimate is itself shrunk toward the league by **CAREER_N = 2** pseudo-seasons, without which a one-season player is blended toward himself and **Tanvir 2008 ranked 3rd of 1,689** ahead of every Kohli and Gayle season; and **the blend feeds `rated_per_ball`, not just the display**, because a card the engine contradicts inside one over is worse than no card. **Check 22** enforces the second and is falsifiable — blend the card alone and **1,689 of 1,689** rows fail. |
| A58 | **The display scale is 70-100, INTEGER, anchored on PERCENTILES not extremes.** A44's 0-100 with a mean of 50 is a statistician's scale; a game scale puts the floor where a weak card is still a card, and it costs nothing in honesty because the map is linear — every gap survives in proportion, which is A44's own argument against percentile. Anchored at the **2nd and 99.8th percentiles** of blended merit: Gayle 2011 and 2012 sit far clear of the field and anchoring on them compressed **55% of all seasons into a five-point band**. On percentiles the spread is **19/40/26/11/3/1%** and four seasons reach 100. Integer rounding was requested and makes ties common — about **56 seasons per point** — which is accepted, since a drafter sees fifteen cards at a time and the ordering survives in `rated_per_ball`. |

| A59 | **An all-rounder is worth more than the sum of his two impacts, and the reason is the DRAFT not the cricket.** A55 made the disciplines add; that was the missing half, not the whole. The drafter is solving a template — 15 cards into keeper 1 / opener 2 / top_order 2 / middle_or_finisher 3 / bowler 5 / open 2 — and a card filling a batting band AND the bowler slot relieves two constraints with one pick. Narine 2024 slots at `opener`, `bowler` or `open`; a pure opener of equal merit fills one. Priced at **ALLROUNDER_RUNS = 5.0** runs/match at a full double share, scaled **continuously** by `least(bat_bpm/18,1) * least(bowl_bpm/24,1)`. **Continuous, not a threshold, deliberately**: a cut at "3 overs bowled and batting enough" qualified 38 of 123 dual-rated seasons and put a cliff through a continuum — **Watson 2015 missed by 0.6 balls a match** while Watson 2011 cleared it. A33's floors and A43's threshold sit in wide empty gaps in the evidence; this quantity has none, so a threshold would be an arbitrary line rather than a rule about evidence. References are the two full shares: 18 balls faced is 3 overs and also the league mean exposure (18.7), 24 bowled is the hard maximum. Declared game-design term, like A57. Narine 2024 95 -> **99**, Watson 2008 -> **98**, five of the top twenty are dual contributors. |
| A60 | **The scale tops out at 99, not 100** — 100 reads as a perfect card and nothing in nineteen seasons is perfect. Purely a display decision: the map stays linear on blended merit and only the upper anchor moves, so every gap survives in proportion as A58 requires. Five seasons reach 99. **Also recorded here, a structural consequence of A54 that is NOT corrected**: a bowler may bowl at most **24 balls** a match against a top-order batter's **43.6**, so bowling exposure is capped near **55%** of batting's ceiling. That is true of cricket rather than an artefact of the denominator, so it stands; A59 partly offsets it for players who do both. |

| A61 | **The four-overseas rule is enforced at DEAL time, capping the SQUAD at four.** §1.1's template said nothing about nationality, so the deck could hand a drafter fifteen cards that cannot produce a legal eleven — measured at **87 of 400 drafted XIs, better than one in five**. Capping the squad at four is stricter than the rule it enforces (a real franchise carries more overseas than it fields) and is chosen anyway **because it makes legality structural**: with four or fewer in a squad of fifteen, EVERY eleven drawn from it is legal and no XI-selection path has to be trusted to find the legal one. Caps of 5 and 6 also give 0 illegal XIs today, but only by relying on the selector — which is the sequence-property mistake A40 already made once by counting slots. Measured by running the draft, not by counting: **400 of 400 XIs legal, 0 uncertain, 0 impossible**, and cap 4 had the *highest* rational guarantee-off completion of the three candidates (99.0% against 98.4% at 5 and 97.6% at 6). **The cost is that the guarantee is no longer inert** — see the revised note under `etl.feasibility`. `is_overseas` is NULL for nobody (A51) so the cap counts a known quantity, and an unknown stays draftable rather than silently spending an overseas place, which is A23's rule at the third and last place the count is taken. `tests/test_draft.py` holds it; all six assertions verified falsifiable. |

| A62 | **Sessions are STATELESS and seed-based; nothing is stored and no account is needed to play.** Ratified 2026-07-31 before any web code exists, because it decides the schema and the API shape and is far cheaper now than retrofitted. `run_draft` is a closed fifteen-pick loop with the drafter supplied as a function; a website replaces that function with a person, so the loop breaks across fifteen HTTP requests and the half-drafted squad has to live somewhere. It lives **in the client**, as a signed seed plus the choice indices so far, and each request replays from scratch. **Verified rather than assumed before ratifying: 200 of 200 drafts replayed EXACTLY** — same squad, same slot assignment, same sequence of franchise-seasons dealt — and the match reproduces from the same seed, so a session is deterministic end to end. Note the RNG stream depends on the choices (a pick changes which cards stay eligible, hence how often the guarantee re-draws), which is why the seed alone is not enough. **State is a seed and fifteen small ints**; it fits in a URL. Costs: full draft replay **3.3 ms**, a fifteen-request draft ~**25 ms** of CPU total, deck **1,689 cards** loaded once at boot in **3.8 s** and held in memory. **Daily challenge and login are deferred, not rejected** — both are additive: a daily challenge is one shared seed per date, and a `results` table arrives with the leaderboard and not before. **Settle before the leaderboard, not after: is a deal random per player or shared per day?** SPEC §11. |

| A63 | **The game is a SEASON: fourteen league matches, a table, then the IPL's own playoffs.** One match cannot separate a good side from a good night. **Opposition is HISTORICAL** — nine real franchise-seasons, each fielding the best legal eleven that squad could put out, drawn uniformly over the 166 like the deck (A10). That is the only opposition the archive can vouch for. **Ten sides at fourteen each = 70 fixtures**, needing a 5-regular "plays twice" graph: circular distances 1, 2 and 5, where 5 is the antipode and names one opponent rather than two (5 = 2+2+1). **NRR charges a side bowled out the full twenty overs** — the competition's own rule, and without it a side improves its run rate by collapsing. **Playoffs are the IPL's, not a bracket**: Qualifier 1's loser drops into Qualifier 2 rather than out, which is most of what the table is FOR — under a bracket, first and third are nearly equivalent. ~3s for 74 matches, done in-request because it is deterministic from the state and therefore cacheable (SPEC 11). `tests/test_season.py`, 12 assertions, all verified falsifiable — one initially guarded the wrong line and the break had to be re-aimed. |
| A64 | **The deal shows the WHOLE squad, greyed, not just the takeable part.** A franchise-season is ~20 men and about half clear A33's floors, so a pickable-only list misrepresents it: Chennai 2010 reads as ten players and you cannot see it had Dhoni once your keeper is named. Everyone else carries the reason — *already drafted*, *overseas quota full*, *no place open*, or for an unrated man **the numbers**. That last wording is load-bearing: **McCullum made 158\* in the first IPL match and is unrated for 2008**, because he played 4 matches and faced 92 balls, under A33's floor. "Not rated" reads as a bug to anyone who remembers it; "4 matches, 92 faced · below the rating floor" reads as the rule. Unrated men are carried as plain records and **never as `Card`s** — a Card is the draft's currency, and one reaching `cards_by_fs` would make an unrated player draftable through the `open` slot. |

| A65 | **A33's gate is REMOVED from the rating; it was never a statement about the player, only about the estimate.** The floors are still right about what they measured — below 100 balls faced or 150 bowled, a per-ball figure is noise. The CONSEQUENCE was wrong: a gated season had no row in the view and so was not draftable, and a squad is ~20 men of whom roughly half are gated. **RCB 2025 won the title and offered nine of nineteen men with no card.** The fix is not a lower floor (A33 measured where the lists stop moving) but to stop reading "we cannot estimate this precisely" as "this player cannot be picked". Thin seasons now get a rating dominated by their prior, which is what shrinkage is for. The gate survives in `player_season_impact.not_rateable_reason`, so why a season is thin stays auditable. **3,333 player-seasons rated, up from 1,689; 4 of 3,337 squad members remain unrated** — the men who never faced or bowled a ball (A27). |
| A66 | **Shrinkage counts BALLS for a per-ball figure and MATCHES for a per-match one.** Removing the gate alone put **Hussey's THREE-MATCH 2008 at 99**, top of the all-time list, and Harshal Patel's ONE-match 2017 at 97 — the exact failure A33 was built to prevent, and it happened *despite* k = 100 balls, because A54 then multiplies by balls-per-match and that is estimated from the same tiny sample. The shrinkage was undone by the step that made the number a card. So the per-match quantity gets its own stage in its own unit: `w = matches/(matches + 6)`. Hussey 2008 goes **99 -> 87**, no season under six matches survives the top fifteen, and a full season is barely touched. |
| A67 | **Reputation becomes a FLOOR rather than a blend.** A57's symmetric 55/45 blend was meant to stop a great player's bad season reading as broken — and it did, but it also dragged GREAT seasons toward the man's average years. Measured: the blend retained only **59.5%** of the spread in merit, and **Kohli's 2025 fell from a merit of 4.03 to 3.25** purely because he has also had lean years. A floor does the job without the cost: a career may lift a poor or thin season and may never pull a good one down. Bumrah stays **80-96** across thirteen seasons and his 2026 still reads 80 rather than 37, which was A57's whole point. |
| A68 | **Estimate parameters where the evidence is adequate; rate everyone.** A65 removed the gate everywhere at once and check 13 failed within minutes: batting `finisher` drifted **0.090 runs/ball** across eras against a 3-SE bar of 0.054. SPEC 8.13's own instruction for that failure is to split the offset by era. **It was checked before it was obeyed, and the instruction would have been wrong.** The same drift on gate-passing seasons alone clears nothing — worst ratio falls **1.65 -> 0.90** — so it is not an era effect but thin seasons, whose band comes from a handful of innings and which are unevenly spread across eras now the Impact Player rule has teams using more players. Splitting would have fitted noise, which A2 refused twice. Season means and cohort offsets are now measured on gate-passing seasons and applied to all. |
| A69 | **The reputation floor must be CONVEX or it stops being shrinkage.** 013 wrote it as `0.85 * career`, which is sign-blind: a below-average player's career merit is negative, so 85% of it is LARGER, and the floor silently lifted every poor player toward the league. **Check 9 caught it on the row it moved** — A Mithun 2009 blended to -0.591 from a merit of -0.695 and a career of -0.721, outside both. Now `0.85*career + 0.15*merit`: still lift-only against the evidence blend, but a genuine convex combination, so a rating can never leave the interval its own two inputs span. |
| A70 | **Migration 010 SAID the cohort offset was coalesced to zero and the SQL never did it.** The comment claimed a LEFT join with a coalesce so an unclassified cohort "arrives uncorrected rather than vanishing"; the code was `centred - cl.cohort_offset`, which yields NULL. It never fired because every cohort had rows, so **for four migrations the guard was decorative and nobody could tell** — each one copied the block forward verbatim. A68 broke it within minutes: offsets estimated on gate-passing seasons leave `tail` with none (A39), so every tail card came back NULL and the draft crashed on `max()` of an empty sequence. **The loud symptom was luck** — a NULL reaching the display would have shown a blank rating and nothing else. **A comment asserting behaviour the code does not have is worse than no comment**: it is a claim a reader will trust. |
| A72 | **The draft's slot template (A6/A40) is retired for something skill-driven: eligibility for each numbered batting position is a career fact about the PLAYER, not a category his card belongs to.** The obvious mechanism was tried first because it needed no new ETL: career `MIN(batting_position_min)`/`MAX(batting_position_max)`, already on `squad_members` since migration 004. It does not hold up — measured against the archive, SV Samson comes out **1-8** and RK Singh (Rinku) comes out **3-8**, both far looser than reputation, because a single outlier innings widens a MIN/MAX envelope forever and can never narrow it back. **Counting replaces enveloping**: a position is real evidence about a player only if he has batted there in at least **`MIN_INNINGS_AT_POSITION` = 5** separate career innings — declared directly by the user, a game-design constant like A57's `REPUTATION`, not a threshold swept against a list. Migration 017 adds `person_batting_positions` (`person_id, position, innings`), derived **career-wide across all nineteen seasons** by `etl.career_positions` in one pass over the same per-innings `batting_order()` scan A4 already established, re-run after any `etl.load`. Measured named examples, matching the user's own ("you cannot play Vaibhav Suryavanshi anywhere but opening," "Rinku Singh only below 4"): **Samson eligible at 1, 2, 3, 4, 6** (not 1-8); **Rinku at 5, 6, 7** (not 3-8); **Suryavanshi at 2 alone** (his one recorded position, 23 innings); **KL Rahul at 1, 2, 3, 4** (not 1-11, which the old envelope gave him). **Check 23** confirms `person_batting_positions` against an independently recomputed aggregate: 2,484 (person, position) rows, 3 named players pinned exactly. |
| A73 | **The draft itself simplifies from "pick fifteen, then arrange twelve" to picking the playing TWELVE directly, one slot at a time — and this is a game-design simplification with a real, load-bearing mathematical consequence, not just a UI change.** The fifteen-then-arrange shape was built, played, and found to be too much work: evaluating fifteen candidates and then solving a twelve-slot arrangement puzzle is roughly twice the cognitive load a direct twelve needs. So a pick now names both a candidate and the slot he bats at (or Impact) in one move, **final the moment it lands** — no bench, no unplace, no rearranging, confirmed directly with the user in preference to keeping that flexibility. Squad size and slot count become the same number, `TWELVE_SIZE = 12`, and `SQUAD_SIZE` is retired. **This collapses the forward check from a bipartite-matching problem into two integer inequalities.** With exactly as many future picks as future open slots — an invariant "final at pick time" guarantees, since every pick fills exactly one open slot and none is ever freed again — a maximally-flexible hypothetical future pick can always be matched to *some* remaining slot (Hall's theorem on a fully-connected bipartite graph), so position eligibility can never be the thing a forward check needs to verify. Only the keeper and bowling-depth COUNTS can still be unreachable, which is what `could_still_complete` checks. **Deleted entirely, no longer needed**: `Twelve`, `_match_positions`, `best_twelve`, `can_field_twelve`, `_wildcard`, `could_still_field_twelve` — all existed to solve "does some arrangement of this fifteen-card squad work," a problem that stops existing once every pick is placed atomically. `order_errors` is kept unchanged as the one independent, from-scratch verifier, precisely because it shares no code with the new incremental check. **The widening rule, in the same pass**: `TAIL_DEFAULT = (9, 11)` becomes `LOWER_ORDER_BAND = (7, 11)` — "any position below 6," confirmed with the user to exclude 6 itself — and now covers TWO cases, not one: a player with no qualifying position anywhere, AND (new) a player whose qualifying positions are ALL already in the lower order (a recognised finisher, a bowler who has batted enough to qualify at 7 or 8) — both widen to the full band rather than staying pinned to a narrow exact set, because both are the same kind of fungible lower-order resource in practice. A player with *any* qualifying position below 7 keeps his exact measured set untouched — the widening only ever loosens where the evidence is silent or already agrees, never where it says otherwise. **Check 24** verifies all three branches independently: 1,540 sub-threshold rows stored undecorated, 940 lower-order-only cards widen to the full band, 1,608 cards with genuine top/middle evidence keep their exact set, every one of 3,337 loaded cards has a non-empty `positions`. **A real bug was found and fixed in the same pass, not a shortcut newly introduced**: `pick_random` had always drawn from the global `random` module instead of the `rng: random.Random` `run_draft` was actually seeded with — invisible under the old fifteen-pick model's slack, but it made `check_12`'s "random" policy stranding rate look non-reproducible until traced down to it. Fixed to draw from `rng`, matching A62's seed-determinism guarantee. **With that fixed, `check_12` still measures a small, genuine, expected stranding rate for the adversarial "random" policy alone** (14 of 2,000, reproduced against seed 7: an actual pick sequence needing a keeper who ALSO bowls at position 10, with 11 of 166 franchise-seasons offering an eligible keeper there and precisely 0 offering one who bowls too — no redraw finds a player who does not exist) — `rational` and `naive` both show **0 of 2,000**, because a policy that manages either requirement at all essentially never reaches the corner a policy actively ignoring both can. This is the wildcard-optimism gap (A31/A61's own framing) recurring in the tighter model exactly as expected, reported rather than engineered away. |
| A71 | **A65 rated every player-season with `balls > 0`. It left one population out: squad members with ZERO balls in both disciplines all season.** Measured at exactly **4 of 3,337** — JP Faulkner 2011, SS Mundhe 2011, VY Mahesh 2009, MS Bhandage 2025. Not A27's excluded 47 (those never get a `squad_members` row at all); these four have a role and a band or usage — the squad was picked to include them — but that season they were run out without facing, or never got an over that survived, or sat out every match they were part of. There is no per-ball evidence to shrink, so a fifth evidence tier is not the fix — that would be A33/A48's mistake in a new place, an individual number manufactured from nothing. What two of the four DO have is a career: Faulkner (six other rated seasons) and Mahesh (three) are rated from `reputation_merit`, the same CAREER_N = 2 shrunk-toward-league average A57 computes for everyone else, evaluated with no current season to leave out. **Mundhe and Bhandage have no rated season anywhere in nineteen years**, so they fall to `scale.lo` — the same 2nd-percentile anchor A58 already uses to fix the bottom of the 70-99 scale — a deliberate arbitrary floor, never a fabricated merit number (A23/A48's rule against defaulting an unobserved value to something plausible). `rated_per_ball` — what the ENGINE plays, not only the card (A45) — converts `blended_merit` through the same two reference exposures A59 already declares, 18 balls/match batting and 24 bowling, since a runs-per-match reputation figure has no per-ball equivalent of its own to borrow instead. Migration 016, a view replacement only. **Checks 6, 9 and 22 all needed real rewrites, not tolerance** — 6 now asserts the exact row count (`playable` + zero-evidence) AND that zero squad_members are uncovered; 9 gained an explicit assertion that reputation-only rows equal their own career merit (or the single shared floor value) rather than silently passing the existing bound check on NULL propagation, which check 9's own bound query would otherwise have done — a silent pass on NULL is the vacuous-check failure this project's standing rule refuses; 22 special-cases the reputation-only branch's integration formula, because the general `rated_per_ball * balls/matches` formula multiplies by `balls = 0` and integrates to zero regardless of what the card claims. All three rewrites verified falsifiable — reverting to the pre-A71 view fails 6 on both counts, and targeted breaks of the reputation and reference-exposure arithmetic fail 9 and 22 respectively. |
| A74 | **The high anchor moves from the 99.8th percentile of blended merit to the 99th; the low anchor (2nd percentile) is unchanged.** Found by counting, not by feel, when asked why 80-rated cards felt rare: at p99.8, **82%** of the 3,337 cards read 70-79, **16%** read 80-89, and only **1.6%** ever reached the 90s. The arithmetic cause is that p99.8 sits at **11.66 raw merit**, a value set almost entirely by a handful of legendary outlier seasons (Gayle 2011/2012, already named in A58) — while the middle 50% of ALL player-seasons spans only **-0.53 to +1.81**, a 17%-wide sliver of the (lo, hi) interval the scale stretches across 70-99. That is why the deck piled onto a single point (74) instead of spreading. **Neither of the two mechanisms that might have been expected to fix this actually does, and that is worth recording so the question is not re-asked.** Season-relative centring (A42/A68) removes ERA drift only, not the shape of the distribution within a season. Reputation (A57/A67/A69) is a FLOOR, not a boost — it can lift a strong career player's thin or bad season toward his own average, but it cannot manufacture a good season out of a player whose career average is itself ordinary, which is most of any squad. **Raising the LOW anchor was tried first and is a wrong turn worth naming, not silently dropping**: the reasoning was that it would push the ordinary middle of the pack up toward 80. Measured instead of assumed, it does the opposite — for any player below the high anchor, d(fraction)/d(lo) = (merit − hi)/(hi − lo)², always negative since merit < hi almost everywhere, so raising lo from p2 to p30 alone would have dropped the median player's card from 75 to 71. Lowering the high anchor is the move that actually works, because the ceiling — not the floor — is the one stretched far past where the data sits. Swept from p99.8 down to p90 and measured at each step: **p99** was chosen as the point that meaningfully thins the 70s band (82% → 68%) and more than doubles the 80s band (16% → 26%) while barely touching the top — 36 cards pinned at the 99 ceiling against 8 today, still a small, genuinely rare group, not the quarter of the whole deck a cutoff as low as p90 would flatten together. Migration 018, a view replacement only. A ratified game-design choice, not a re-measurement of anything — same category as `REPUTATION`, `ALLROUNDER_RUNS` and the anchors themselves. |
| A75 | **The card's all-rounder ICON quietly stopped meaning anything the day A65 rated everyone, and nobody reconciled it.** `web.app._kind` derived the icon from `has_bat and has_bowl` — true the instant BOTH disciplines carry ANY rating — reasoning (correct when written, `c0ba38a`) that a man rated in both is a genuine dual threat even where A26's averages call him one-dimensional. That held only because a discipline was rated at all exclusively after clearing A33's volume floor (100 balls faced / 150 bowled), so "has a rating" meant "substantial workload." **A65 deleted that floor everywhere**, so a single ball faced or bowled now earns a rating too (heavily shrunk, but non-null), and the icon test silently degraded into "faced or bowled a ball at least once in his life." Found by inspection, confirmed by measurement: Bumrah, Hazlewood and Cummins seasons with single-digit balls faced all read `allrounder`; of the **1,816** cards the old test called all-rounder, only **284** actually clear A26's calibrated per-match averages — the other **1,532** are pure bowlers (1,277) or batters (253), plus 2 keepers, mislabelled off a token ball in the discipline they don't play. **The fix is not a new, stricter volume rule — one already existed and was being bypassed.** `_kind` now returns `card.role` directly, which already IS the calibrated test (A26); there was never a second criterion to invent, only a duplicate uncalibrated one to delete. A genuine dual-threat season (Narine 2018/2024/2025, real volume in both) clears A26 on its own and reads `allrounder` unchanged. No migration — application code only. `tests/test_card_kind.py` pins it, both regression cases verified to fail under the deleted logic: a rule living in application code rather than a table CHECK is unprotected without a test, per the standing rule above. |
| A76 | **A72/A73's per-exact-position eligibility is replaced entirely by a single career-based ROLE per player — `top` (1-3), `middle` (3-5), `finisher` (5-7), `tail` (8-11)** — at the user's direct request, to make picks more competitive by narrowing a player to one coherent job rather than the widest set his exact measured positions happened to support. **Measured before building anything, not assumed: a career-only single role throws away real evidence for a lot of players, not a rare few.** 124 of 384 players with any qualifying position evidence (32%) have genuine, unambiguous innings volume in two or more of the proposed bands — Pant, Samson, Suryakumar Yadav, Rahane, Ishan Kishan, Shreyas Iyer, both Pandyas, Rayudu, Dube, Dinesh Karthik, Jadeja among them. A hand-maintained "rare outlier" list (Narine as the paradigm case) would miss nearly all of this population, and per A43's own standing objection to name lists, would go stale the day the archive is revised. **The fix: try each SEASON's own evidence first, fall back to CAREER evidence, and only then to a fixed default** — the same season-then-career cascade A57/A67/A71 already use for ratings, needing no name list at all. **The "no evidence anywhere" bottom tier is not actually evidence-free, and defaulting it to `tail` uniformly would repeat A23's mistake in a new place.** It splits into two populations. `role == 'bowler'`, zero career batting innings: `tail` is corroborated by A26's own bowling-workload test, not a guess — measured, 95 of 1,661 bowlers (5.7%) never bat at all. `role` is `batter`/`allrounder`/`keeper` but below the innings floor: this population can never have literally zero evidence (clearing A26's ball-volume bar requires having batted at least once), and measured, 85-92% of their actual innings are OUTSIDE the tail. A third cascade tier — the thin signal used whenever `role != 'bowler'` — rescues these domestic short-career batters instead of defaulting them to tailender. **The algorithm is NOT "find the single most-frequent exact position, then check a secondary position only at the boundary" — that was tried first and is wrong.** Rohit Sharma's single most common exact position across his career is 4 (86 innings) even though he is a career-defining opener, because his top-order innings split across two positions (1: 74, 2: 52) while his middle-order innings land on one. Comparing AGGREGATE band totals directly (167 top against 149 middle) decides him correctly with no exact-position modal step at all. Positions 3 and 5 (the top/middle and middle/finisher boundaries) are each allocated to whichever neighbour has the larger UNAMBIGUOUS total — ties favour the earlier band, which is also why position 3 alone resolves to `top` and position 5 alone resolves to `middle` with no special-cased convention needed. **A second bug was caught building the calibration table, not assumed away**: the first implementation compared position 5's allocation against a `middle` total already carrying position 3's vote, so a heavy top/middle career (KD Karthik: 30 at position 3, 66 at 4) could inflate `middle` enough to also win position 5's comparison against `finisher` — reading a specialist finisher as middle order. Fixed by freezing the unambiguous baseline before either boundary position is allocated. **22 real, publicly undisputed careers, spanning all four bands, all classify correctly** (`etl/batting_roles.py`'s `CALIBRATION`, the same shape `etl.roles.CALIBRATION` uses). `MIN_INNINGS_FOR_ROLE = 5`, the same value A72 used for one exact position, applied identically to a band's aggregate total at both grains. **SP Narine's actual season-by-season sequence is the demonstration, not an assertion.** Career-wide he reads `top` (68 top-band innings against 36 tail) — the wrong answer for every one of his genuine tail seasons, which is exactly what the cascade fixes: 2013, 2014, 2022 and 2023 all resolve to `tail` from their own season evidence; 2017-2020 and 2024-2026 resolve to `top` from their own. **One honest, disclosed limitation, not glossed over**: his three thinnest, earliest seasons (2012: 2 innings, 2015: 1, 2016: 4) never clear the season floor and fall through to the career answer (`top`) — the cascade's own limit showing up rather than a wrong answer hidden, and a vanishingly rare intersection (extreme season-switcher AND a season too thin to speak for itself) not worth chasing with more complexity. **New table, migration 021: `person_season_batting_positions`** — the per-season twin of A72's `person_batting_positions`, from the SAME scan (`etl.derive_squads.batting_positions()`) `etl.career_positions` already ran, just persisted at both grains instead of one (A19). 6,616 rows. `Card.career_positions` is renamed `Card.positions` — a plain field now, nothing left downstream to widen — and `MIN_INNINGS_AT_POSITION`/`LOWER_ORDER_BAND` are deleted outright. `web/app.py`'s `/api/meta` loses `lower_order_default`: there is no longer a single default band a constant could name. **Check 24 is rewritten under the same number** to verify the new cascade's WIRING (an independent recompute from the raw tables reproduces every card's `positions` exactly); the algorithm itself is owned by `tests/test_batting_roles.py`, no database needed, exactly as `role_for` is owned by `tests/test_squads.py`. **Check 6's trap fired on the new table exactly as designed**, and the fix is a genuinely new checkable fact: `person_season_batting_positions` and `person_batting_positions` are two aggregations of the same scan, so their grand totals (19,111 innings) must be identical, and now are asserted to be. **The draft-completion gate (A40/A61/A73's own methodology) was re-run, not assumed to still hold.** 2,000 drafts x 3 policies, seed 7: `rational` and `naive` both complete 100% (0 failures), matching A73's exact target; the adversarial `random` policy strands at **0.5% (10 of 2,000)** — slightly BETTER than the pre-A76 baseline of 0.7% (14 of 2,000), because the new fixed-width bands (3-4 slots each) give most cards a WIDER eligible range on average than the old narrow per-position sets did. |
| A77 | **A genuine wicketkeeper-batter stays draftable as a keeper in every season of his own career, not just the ones the archive happens to attribute the gloves to.** `squad_members.role` is per-season (A24/A52), so a career keeper who kept in one season but is recorded as a plain `batter` in another (someone else kept that year) could not satisfy a squad's keeper requirement off that card at all — the Buttler-2022 shape, and the motivation was named directly: "otherwise there is a dearth of quality keeper batters." **New `Card.keeper_eligible: bool` field, replacing the dead `career_keeper`**, set at load time to `role == "keeper"` THIS season OR this `person_id` appears with `role = "keeper"` in ANY other season of `squad_members` — a plain distinct-`person_id` query, not a re-derivation of A52's own keeper-resolution work. **The season's own `role` is never touched** — a batting-season card still reads `batter`, correctly, since nobody else's business is to relabel a season the archive itself resolved; only whether the card can satisfy a squad's keeper slot widens. All **8** legality call sites that read `card.role == "keeper"` for squad-completion purposes (`order_errors`, `could_still_complete`'s callers, `eligible`, `pick_rational`'s sort key, `run_draft`'s bookkeeping, `report`'s stats) move to `card.keeper_eligible`; `game/__main__.py`'s `viable()` deliberately stays on strict `role == "keeper"`, since that check is about whether a HISTORICAL opposition eleven really took the field (A63), not squad legality, and the career fallback is a human-drafter rule with no business there. Two falsifiable tests pin the shape, both verified to fail against a revert to `role == "keeper"` alone. |
| A78 | **Whether the Impact Player plays is decided AT THE INNINGS BREAK, informed by the real first-innings result — never pre-match.** Real IPL usage skews heavily toward a BATTING swap (a bowling swap is capped at 4 overs/24 balls, a batting one is not) and is a live, informed call; our own pre-match model measured the *opposite*: **62.5% bowling swaps against 23.1% batting**. Root cause was structural, not a bad weight: the old `decide_impact` ran an argmax between a batting gain (measured against a genuine all-rounder, `_pure_bowler_678`) and a bowling gain (measured against the side's OWN WORST bowler, `_weakest_bowler`, to dodge A31's own sentinel-comparison trap) — an unfair pair of references that made bowling upgrades look artificially larger regardless of the matchup. **The fix removes the argmax rather than reweighting it**: at the break each side has exactly ONE discipline left (whichever innings it has not yet played), so there is nothing left to compare across. `decide_impact(side, opponent, discipline, first)` now takes the real `Innings` just played; the situational term compares its `runs` against a declared `IMPACT_PAR_SCORE` (170, close to check 20's own measured 170.4) instead of the opponent's pre-match averages. **Incorporates a second, independently-specified requirement in the same pass: a batting Impact Player always replaces a TAILENDER WHO BOWLS, and bats where HIS OWN band says, not in the vacated slot.** `_tailender_bowler` restricts the swap-out candidate to positions 8-11 (A76's own `tail` band) who also bowl, weakest bat among them — never a bowling all-rounder batting higher up. `_insert_batting_impact` then removes him and inserts the Impact Player at `min(impact.positions) - 1`, cascading everyone from there down to the vacated slot down by one place: a top-order Impact bats at the top and the order below him compresses, exactly how a real side slots a specialist in rather than parking him in the dropped man's exact spot. A bowling swap stays a straight swap (`_apply_bowling_impact`) — A50's five-bowler floor changes only in timing, not in kind: `_bowling_depth_shortfall` now also runs BEFORE whichever innings a side bowls FIRST in (no break has happened yet to decide it there), so a legal twelve that needs its Impact Player to reach `BOWLERS_IN_TWELVE` locks him in from over one rather than waiting for a break that would leave `attack()` short in the meantime. **Re-measured, not assumed fixed**: against 1,500 real historical-side matchups, the structural change alone moves the split from 23.1%/62.5% to **39.9% bat / 60.1% bowl of plays** (59.5% overall play rate, down from ~85.6%, because each side now gets one try instead of two). The residual bowl-lean is no longer an unfair-reference artifact — an instrumented breakdown shows batting sits out because its own raw gain clears `IMPACT_SIT_OUT_BAR` less often (52.5% of the time) than bowling's does (28.5%), which reads as a property of the deck's leftover-player pool (extras tend to be modest batting reinforcements) rather than a bug in the comparison. **Left unresolved on purpose**: `IMPACT_SIT_OUT_BAR`/`IMPACT_SITUATIONAL_K` are declared game-design constants (A57/A59's own category), not fit to this number, and were not retuned to chase the real-world ratio further. The external contract to `web/session.py`/`web/app.py` (`NeedImpact.match.discipline`/`.human_bats_first`) is unchanged — only the internal decision and substitution logic moved. `tests/test_season.py` rewritten for the new signature and the tail-zone boundary (a deliberately weaker-batting card at position 7 proves the boundary is respected, since including it would wrongly win the "weakest" comparison). |
| A79 | **A78's break-time toss + Impact engine comes to rooms, with three narrow jobs rather than one decision routed to whichever seat happens to own it — confirmed with the user directly.** The TOSS WINNER calls bat/bowl for their own side and nobody else's (a CPU-owned winner auto-resolves to `TOSS_DEFAULT_ELECTS`, same as everywhere else). The HOST decides every Impact Player choice, for EITHER side, in every match — simulation decisions are the host's job, not the owning player's, which is what keeps a room from ever needing to work out who currently controls "the other side" mid-match. Only the HOST advances from one match's result to the next, so the pacing of reveals is one knob, not a race between however many seats are watching. **`game.season.play_open` generalises A78's mechanism from one fixed tracked human to two PEER sides** — either can win the toss, either can have a break-time decision — with new `_OpenMatchNeedsToss`/`_OpenMatchNeedsImpact` pause exceptions carrying `Side` IDENTITY rather than a player_id, since `game.season` has no concept of one; `web/room_match.py` (new module, migration 022's `rooms.match_moves`, a second shared move log mirroring `rooms.moves`'s own replay-from-scratch shape) does the pid mapping and all authorisation, keeping `game.season` itself free of any web concept. Fixtures for `final`/`cup`/`league` are built INCREMENTALLY, not precomputed, because a cup's final and a league's playoffs both depend on results the earlier fixtures haven't produced yet at replay start. **A real bug was found timing this, not assumed away: a CPU-owned toss winner never auto-resolved** — `_RoomMatchCursor.next_toss` unconditionally waited on a move nobody could ever submit, since nothing prompts a CPU seat; fixed by checking ownership before consuming a move at all. **A ten-team league's round-robin is deliberately NON-interactive, a measured scope decision, not a style choice**: `replay_room_matches` replays every already-resolved fixture from scratch on every call (A62's own cost, the same profile solo's `replay_season` already accepts), and pacing all seventy round-robin fixtures the way `final`/`cup` pace their few matches was timed at multiple SECONDS per step by fixture 20 and climbing — full suite 49.5s before the fix, would have been minutes for one real league room. The round-robin now plays straight through automatically (A78's engine decides every toss and Impact call for all seventy, nobody is asked live); the four PLAYOFF matches — the dramatic part — still pause normally, so the host's pacing control is reserved for where the volume actually allows it. Full suite **8.1s** after both fixes. `tests/test_room_match.py`, reusing `tests/test_rooms.py`'s own `FakeConn`, pins toss/Impact/advance authorisation (wrong seat refused, right seat/host succeeds), the round-robin's one-shot table-and-playoffs transition, and a positional move-log integrity check. **Verified live against a running server for the `final` format**: wrong-seat toss and non-host Impact calls both refused with a 409, the correct actors' decisions both progress the match, and a full room completes with a real scorecard — this pass also caught and fixed a frontend copy bug (home always bats first in this engine, so the "who posted the first innings" text was reading the wrong side on the "bowl" discipline). |
| A80 | **The journey card comes to rooms, one seat at a time.** `game.season.JourneyStats`/`journey_stats()` cannot be reused directly — it hard-requires a `Season` object's `.table`/`.playoffs`/`.champion`, and a room has none of the three (A79: every fixture, round-robin or knockout, lives in one flat `results` list keyed by `.stage`). `web/room_match.py` gets its own `RoomJourney`/`room_journey(room, replay, pid)`: played/won/lost/tied counted by SCANNING `replay.results` for fixtures involving `pid`, not by reading a `Standing` row, because `final`/`cup` never build one at all — a scan is the one approach correct for all three formats uniformly, including league. Wired into `RoomMatchOut` (`web/app.py`) under the exact same field names and "null until complete" contract as solo's `SeasonProgressOut`, reusing `_journey_entry()` verbatim for the squad list — this is what let the frontend's existing `drawJourneyCard(d)` be reused almost unchanged, needing only an optional `header` parameter (`'ROOM ' + code` in place of solo's `'SEED ' + seed`) and a "final" format is generalized to have a `champion_pid` too (a one-match format's champion is just its winner — no fabricated one on a tie, and no per-format special case needed downstream). **A real identity bug was found by the new tests, not assumed away**: `room_journey`'s first draft resolved a `Result.home`/`.away` back to a player_id by calling `_sides_with_pid(room, deck)` AGAIN and comparing by `is` — but that call builds brand-new `Side` objects every time, so the identity check against the ALREADY-BUILT `Result` (from an earlier, different `_sides_with_pid` call inside `replay_room_matches`) silently failed every time (`StopIteration`), exactly the `Side`-has-no-`eq=False` trap this codebase is otherwise careful about (A79's own docstring already names it, and it still happened one function later). Fixed at the root, not patched at the call site: `RoomResultEntry` now carries `home_pid`/`away_pid`, resolved ONCE inside `_Driver.play_fixture` against its own single consistent `pairs` list, at the moment `side_a`/`side_b`'s identities are unambiguous — `_standings_table` simplified to read the same stored fields instead of its own (already-correct, but fragile-by-coincidence) `_pid_of` calls. **Verified live against a running server for the `final` format**: both the champion's and the loser's own journey cards render correctly from the SAME completed match, each showing that seat's own twelve, its own top scorer/wicket-taker, and the right champion/non-champion banner — proving the per-seat split, not just the per-seat schema, actually works. |
| A81 | **A room's Impact Player decision is gone, not merely hidden — `game.season.play_open` now resolves it via `decide_impact` unconditionally, for either side, exactly like `play()` (the fully-automatic engine) always has.** A79 gave the host a manual override — pick a slot, or decline — for either side's substitution, reasoned as "simulation decisions are the host's job." That stopped being true the moment A78's `decide_impact` was trusted enough to run the league's own seventy-fixture round-robin completely unattended: acting as a proxy for a substitution on a team that isn't even the host's own was never a meaningful choice, only friction. Confirmed directly with the user ("no need to give choice of impact player now that the logic is fixed"). `_decide`'s slot-override branch and `_OpenMatchNeedsImpact` are deleted outright — `play_open`'s `moves` protocol now carries only `next_toss`, since toss remains the one decision worth keeping (confirmed separately: "keeps the toss fun"). Solo play is untouched: its own human Impact choice lives in the structurally separate `_play_human_match`/`_next_impact`, which `play_open` never touches — confirmed by grep, `play_open` has exactly two callers and both are in `web/room_match.py`. Every room-side trace of the old choice is removed rather than deprecated: `RoomImpactIn`, `RoomPendingImpactOut`, the `POST /match/impact` route, `submit_impact`, `roomSubmitImpact`, and the `impact` branch of the old `roomPendingHtml`. `tests/test_season.py`'s three `play_open`-Impact tests (pause-and-resume shape) are replaced by two proving the new unattended behaviour: both sides substitute automatically with no move supplied at all, and a side with no Impact Player never substitutes regardless. |
| A82 | **Rooms advance a ROUND, not a fixture — independent matches within one round resolve in true parallel, and a player's own tournament can end well before the room's does.** Confirmed with the user in three parts, all realised together because they share one root cause: the old `_Driver` walked fixtures strictly sequentially, so a cup's two semis (which depend on nothing but each other's absence of a dependency) were forced through one at a time, each gated behind its own host "advance" click before the next was even BUILT — and a losing seat had no signal of their own elimination until the room's global `complete` flag flipped, which could be arbitrarily later. **The fix: `room.match_moves`' toss entries carry a `"stage"` tag instead of relying on flat position** (two fixtures' tosses can now land in either order — position no longer identifies which fixture a move belongs to), and `"advance"` moves carry the ROUND's own label. `_Driver.resolve_fixture` never lets one fixture's still-pending toss block a sibling's: every fixture in an open round is attempted on every replay pass, mixed pending and resolved, exposed as `RoomMatchReplay.current_round`. Migration 022 stays immutable; **migration 024** corrects its now-wrong column comment (flat-positional, Impact-included) the same way migration 005 fixed 003's (A22). **`eliminated_pids` is computed from `game.season.run_playoffs`/`run_cup`'s own bracket adjacency, not a new design**: cup semi losers are out immediately; a league's non-top-4 are out the INSTANT the round-robin table settles, before the playoffs round even opens; the Eliminator's loser is out immediately but Qualifier 1's loser is NOT (the real IPL second-life rule, surviving to Qualifier 2); Qualifier 2's loser is out; the final's loser lands in `eliminated_pids` the identical way as anyone knocked out earlier, so no screen anywhere special-cases the very last match. `room_journey`'s gate changes from `replay.complete` to `pid in eliminated_pids or pid == champion_pid` — one line, since its pid-scanning body was already format-agnostic — which is the whole mechanism behind an eliminated player's journey card being available before the room finishes. **Frontend reuses the solo reveal engine (`OVER_STEP`/`startOverStepper`/etc.) completely unmodified**, via a new room-side orchestration layer (`roomEnterReveal`/`roomSubmitTossReveal`/`roomStartReveal`) and a small shared `handleTossChoice` dispatcher so both modes' `#tossScreen` buttons work without touching solo's own `submitToss` — a room viewer is never in more than one currently-open fixture at once, so the existing singleton stepper needs no per-fixture instances. A spectating or already-eliminated player sees every fixture in the current round as a plain instant scoreline, never the animated stepper, which is reserved for a fixture the viewer is actually in. **Verified live against a running server, not only unit-tested**: a real 4-seat cup room resolved its two semis independently in either order (Semi-final 2 answered and completed while Semi-final 1 sat untouched and still pending); a real 10-seat league room opened Qualifier 1 and the Eliminator simultaneously in one round, both auto-resolving since every side involved was CPU-controlled, with the Eliminator's loser reading `you_are_out=true` immediately while Qualifier 1's loser read `false`; the league's six non-qualifying seats already read `you_are_out=true` the moment the round-robin table settled, `current_matches` empty, the playoffs not yet even opened; and an eliminated cup player's full journey card (record, top performers, squad) rendered correctly while the room's own `complete` flag was still false. `tests/test_room_match.py` rewritten for the new model, 19 -> 23 tests; full suite 374 -> 377, 21/21 validation checks unaffected (no ETL/ratings table touched). |
| A83 | **The reveal animation for a room's own LAST fixture was silently skipped, for both sides, in every format.** `replay_room_matches` gives every round a paused, advance-gated stopover EXCEPT the one that also completes the room — the same call that resolves the Final (or `final` format's only match) flips `complete` and empties `current_round` in that same response, so a viewer's poll, or even the toss-caller's own request, had nowhere left to find that fixture and jumped straight to the scoreline instead of playing it out. **Confirmed identical in all three formats** by reading `replay_room_matches`'s own final branch: `final` (its only match), `cup` (Final after Semi-finals), `league` (Final after Qualifiers/Qualifier 2) — verified live for `final` and `cup`, both the toss-caller's own response AND the passive side's next poll, and structurally for `league` via the same `FakeConn` harness `tests/test_room_match.py` already uses (a real 10-seat draft is minutes of remote-DB round trips; the replay layer itself is format-generic and needed no format-specific live proof once the mechanism was shown identical). **Fixed frontend-only**: `roomMyMatchToReveal` (`web/static/index.html`) falls back to `m.results` — which already carries the fixture, full innings data included, the instant it resolves — whenever the fixture is no longer in `m.current_matches`. One shared helper feeds both `showRoomMatch` (the passive poller) and `roomSubmitTossReveal` (the side that called the toss), so the two can't drift on where a fixture's result is still reachable. **No backend change, no migration** — `results` already carried everything needed; nothing was missing from the wire, only unread. Three tests pin the data contract the fix depends on (`test_the_final_rooms_only_fixture_is_reachable_via_results_once_it_completes` and its cup/league twins) — not new backend behaviour, but a pre-existing invariant that the frontend fix has now made load-bearing, and which nothing previously verified. |
| A84 | **A room can be replayed in place once it finishes, not just abandoned for a brand new code.** Solo already had this for free — "Draft again" on `#result` calls `newDraft`, which never touches the persistent client-side `DRAFT_MODE` global, so a fresh draft under the same mode was already one click away with zero backend involvement. Rooms had no such path: once `status` reached `'complete'` or `'failed'` there was no way back, per `web/rooms.py`'s own `Room.status` comment and confirmed by grep (`rematch|play_again|reset_room` — zero hits) before writing any code. **New `rooms.play_again(conn, code, player_id)`**: any seated player, not host-only, resets the SAME room row in place — same code, same host, same format/timer_seconds/draft_mode, same real seats, but a fresh seed (so replaying is a genuinely new draft and match, not a rerun of the one just finished) and both move logs cleared. CPU seats are dropped rather than carried over, for the same reason `leave_room`/`kick_player` already free a departing seat: `start_room` refills whatever's empty when the host next clicks Start, so keeping last game's CPU count would misrepresent who actually showed up this time. **Idempotent by construction**: a click landing after another seat's own reset already went through sees `status == 'lobby'` and returns immediately rather than re-rolling the seed a second time, so several players clicking within the same couple of seconds converge on one shared lobby instead of racing — `_load_room`'s existing row lock is what makes that convergence safe. **A real fidelity gap in the test harness was found and fixed, not glossed over**: `_save_room`'s SQL deliberately excludes `seed`/`format`/`timer_seconds`/`host_id`/`draft_mode` from its `on conflict do update set` clause ("set once at creation, never updated"), so `play_again`'s seed change needs its own dedicated `UPDATE` — but `tests/test_rooms.py`'s `FakeConn` didn't enforce that exclusion at all, silently overwriting the whole row on every insert. That gap would have let the seed-reset test pass for the wrong reason (reading back the in-memory object's own already-mutated `.seed`, never proving the write reached storage) even with the dedicated `UPDATE` deleted — caught by writing the test against a FRESH RELOAD rather than the returned object, verifying it actually failed with the fix removed, and only then fixing `FakeConn` to mirror the real `ON CONFLICT` semantics exactly. Frontend: a "Play again" button (lead action, same treatment as solo's own) on every room completion screen (`final`/`cup`/`league` alike) calls the new `POST /api/rooms/{code}/play-again`; the OTHER seats need not click anything themselves — `pollRoom` already never stops polling a `'complete'` room, so their very next poll picks up `status: 'lobby'` and routes them back to the lobby screen automatically. Verified live end to end: a real 2-seat room driven to a finished match, reset via the button, landed both seats back in the identical lobby (same code, same two names, same settings), and a second full draft-and-match cycle in the SAME room completed cleanly. Seven new tests in `tests/test_rooms.py`, all verified falsifiable. |
| A85 | **A room's "filler" seats stop drafting and become real historical squads instead, confirmed directly with the user.** `start_room` filling empty seats with a "CPU" that then drafted its own fantasy twelve turn-by-turn from the SAME shared card pool humans draw from meant a filler's pick could block a real player from a card they wanted, when the filler wasn't standing in for anyone — it only existed to complete the bracket. The user's own proposed fix, confirmed as the direction to build: give a filler seat a real historical franchise-season's squad directly, never a live draft turn. This is *more* consistent with the rest of the app than the design it replaces, not a bolt-on special case — solo's own season opposition is already built exactly this way (A63), so a filler room seat becomes a room-scoped instance of `game.season.historical_sides` (the same `opposition_xi`/`opposition_twelve`/`viable` pipeline, `game/__main__.py`), not new cricket logic. **A filler's squad is a pure function of `(room.seed, filler count)`, independent of `room.moves` entirely** — exactly like solo's opposition is independent of what the human drafts, so it can legally overlap a human's own picks and never competes with them for a card, and needs no new column or migration (derived fresh every `replay_room` call, A62's "resolve on read"). Filler seats never occupy a turn in the snake order any more: the draft proceeds over drafting (non-filler) seats only and completes once THEY reach `TWELVE_SIZE` picks each — a "final" room with one human now completes in 12 moves instead of 24, a "cup" with two humans in 24 instead of 48. `pick_afk` (the timeout auto-pick for a real human's own turn) is untouched — it only ever fires for a seat currently occupying a turn, and a filler never does; that distinction was the user's own, drawn explicitly to keep AFK cover for an inactive PLAYER separate from a pure bracket-filler. `_resolve_cpu_turn`/its dispatch branch in `_resolve` are deleted outright, not merely bypassed — there is nothing left for them to catch. `room_sides` is the one seam that overrides a filler's displayed name to its historical squad's own (`SeatProgress.historical_name`, set once in `replay_room`), so it shows up correctly everywhere downstream: the draft-lobby roster, the match-phase `Side` naming in scorecards and results, AND (**a real gap found live, not assumed**) the "current fixture" a_name/b_name and the champion banner in `web/app.py`'s `_room_match_out`/`_room_current_match_out`, which read `room.players` directly and still showed the raw internal placeholder ("CPU 3") until both were routed through the same override. Seven new/rewritten tests in `tests/test_rooms.py`: legal historical elevens (checked against `viable`, the rule the opposition path is actually built to satisfy — NOT `order_errors`, A76's batting-position rule for a DRAFTED twelve, which a squad never built against `card.positions` fails by construction and was the first thing this pass got wrong and had to fix), distinct franchise-seasons among fillers, a filler's squad unaffected by what humans draft, and — checked deterministically against `eligible` rather than hoping a real deal happens to overlap by chance — a human CAN legally draft a card that also appears on a filler's historical squad. All verified falsifiable. Verified live: a "cup" room with one human showed all three fillers' real, distinct franchise-season names and full elevens immediately after `start_room`, the human's own 12 picks alone completed the room, and the resulting tournament (semifinals auto-resolving, since both sides there were CPU-owned) named its actual champion correctly by squad name through to the final banner. |
| A86 | **A league room's 70-match group stage becomes host-paced, one fixture at a time, on the same "outcome already decided, revealing is presentation" argument A82's round-based engine already established for knockout rounds.** The round-robin used to resolve completely, silently and instantly the moment drafting finished — A79's own reasoning at the time ("multiple seconds per step and climbing") measured a cost that A57's later `_ROUND_ROBIN_CACHE` (keyed on `(room.code, room.seed)`, storing the RNG's own `getstate()`) had already made one-time rather than per-poll, so the premise that justified never pacing it was stale by the time the user asked why they couldn't watch it unfold. **The cache's own determinism is what makes pacing free**: the round-robin's OUTCOME is fixed the instant it is first simulated, so "how much has the host revealed" is a genuinely separate, new, host-owned cursor (`{"kind": "league-reveal", "through": N}` in `room.match_moves`, `_league_revealed_count` taking the max recorded `through`) layered on top of an already-decided result — it needs no new RNG-state handling at all, only a slice. `_round_robin_results` changes what it returns (the raw fixture list, not a precomputed table) so the table can be built fresh from whatever prefix has been revealed; the league branch of `replay_room_matches` returns a new paused state (`league_progress=(revealed, total)`, `league_next` = the fixture waiting at the cursor) whenever `revealed < total`, and only falls through to the existing top-4/elimination/advance-gate logic once the host has caught the cursor all the way up. **`through` is the target cursor, not an increment** — "Continue" sends `revealed + 1`, "Skip ahead" sends `total`, so both are one call to the same `advance_league_reveal` mutator, idempotent on a `through` at or below the current cursor rather than erroring (A84's `rooms.play_again` convergence precedent: a double-click or retry sending the same value twice is a real, expected case here, not a bug). The frontend reuses the exact playoff reveal engine (`roomEnterReveal`→`roomStartReveal`→`startOverStepper`) completely unmodified for a league fixture — `league_next_result` is shaped exactly like any `results` entry via the already-existing `_room_result_out` mapper, so nothing downstream of the trigger needed to know it was watching a round-robin match rather than a knockout one. A round-robin fixture isn't participant-scoped the way a knockout one is, though, so the existing per-participant `ROOM_REVEALED_STAGE` tracking (every league entry shares the literal stage string `"league"`, non-unique) couldn't gate it; a new client-side counter, `ROOM_LEAGUE_REVEALED_THROUGH`, does instead. **Verified falsifiable**: `_league_revealed_count`'s `max()` deliberately broken to `sum()` made `test_league_reveal_pacing_does_not_change_the_final_outcome` and `test_league_next_always_matches_the_fixture_at_the_current_cursor` fail exactly as expected, then restored. **Verified live against a running server, not only unit-tested, and slowly**: this dev session's local Neon connection was heavily contended from the day's testing load, so several requests against the same room took 10-20s and one genuinely stalled long enough to warrant a server restart — a pre-existing `SELECT ... FOR UPDATE` row-lock property of `_load_room` (every request against one room code serialises through it) that a real single-clicking user would never hit, and unrelated to this feature's own logic, which the restart and a clean retry confirmed. Once clear: a fresh room correctly showed "League: 0 of 70 played" with an empty table and Continue/Skip ahead buttons; a real click played the actual ball-by-ball reveal animation (`LEAGUE · BH BATTING / 37/0 AFTER 5 OVERS`) and landed on "League: 1 of 70" with an exactly-one-fixture-counted table; a second click repeated the cycle to "2 of 70"; and Skip ahead jumped straight to the full settled table (14 played each, real points/NRR) with no animation played for what was skipped, `advance_ready`/`you_decide_advance` correctly flipping true, and the existing Qualifiers→Qualifier 2→Final pipeline completing normally to a crowned champion afterward. Six new tests in `tests/test_room_match.py` (pacing-vs-skip-ahead outcome equality, elimination staying empty mid-reveal, host-only, idempotent-on-repeat, refused-past-total, cursor-never-skips-or-repeats) plus four existing tests updated for the new paused-reveal step; full suite 377 → 407 tests, 21/21 validation checks unaffected (no ETL/ratings table touched, no migration). |

| A87 | **A room's results screen stopped dumping every match in one flat, undifferentiated list, confirmed directly with the user as "illegible" and "arduous to scroll".** A completed 'league' room showed the table, then ALL 74 results (70 group-stage + up to 4 playoff) as identical rows, THEN the champion banner at the very bottom -- measured at 4,163px of scroll on a 900px viewport (4.6 screens) before a viewer could even learn who won. Worse, `stageRow`'s badge was `r.winner ? 'W' : 'T'` -- unconditionally green for ANY decided match, home or away, yours or not -- so a wall of 74 rows read as 74 wins regardless of who actually won each one, the second half of "confusing". **The fix mirrors solo's own already-accepted result screen exactly** (`renderTableAndForm`'s `yourForm` / `renderVerdictAndBracket`'s `bracket`), which never shows the whole round-robin either: `roomYourResults(m)` filters to the CALLER's own group-stage matches only (`stage === 'league' && yours`, ~14 of 70), `roomPlayoffsHtml(m)` shows every playoff-stage match (`stage !== 'league'`, small and universally relevant) grouped under its own stage header with the margin line, exactly like solo's bracket. Both need to know whether the caller's own side batted home or away to badge a personal win/loss rather than a generic one -- `ResultOut` gains `you_home: bool | null`, computed in `_room_result_out` from `entry.home_pid`/`away_pid` (NOT `a_pid`/`b_pid`, which the entry's own docstring already warns aren't the same pair). The champion banner moves to the TOP of the 'league' complete screen, matching 'cup'/'final' and solo's own report-first layout, so the outcome needs zero scrolling to see. Result: the same room now scrolls ~2 screens total, table and "1 won" personal form visible together above the fold, playoffs grouped by stage below, actions reachable immediately after. **Verified live against a running server for both formats**, not only reasoned about: a completed 10-seat league room (Kolkata Knight Riders 2022 champions) rendered the banner first, a 10-row table beside a 14-row personal form with correctly red/green W/L badges (previously always green), and a 4-fixture playoffs bracket with neutral dots for the caller's own non-involvement (they finished 10th); a completed 4-seat cup room (Kings XI Punjab 2008 champions) rendered a correctly RED "L" badge on the caller's own lost semi-final and neutral dots on the other two matches they weren't part of. `stageRow` deleted outright, no longer called anywhere. Full suite unaffected (407 passed, 1 skipped; 21/21 validation), since nothing here touches ETL, ratings, or game logic -- a web-layer rendering fix only, no migration. |
| A88 | **The Orange Cap and Purple Cap -- the IPL's own real season-long top-scorer and top-wicket-taker awards -- fill the empty space a ten-row standings table leaves beside a fourteen-row "your matches" panel, confirmed directly with the user.** No accumulator anywhere in the codebase summed a stat across every side in a tournament: `JourneyAccumulator` (`game/season.py`) and its room-side twin `room_journey()` both existed purely to track ONE nominated side's own twelve, fed only when `track is home or track is away` -- every other side's own `Innings` objects were already being computed in full (`Result.home_innings`/`away_innings`, every match) and simply never read. **New `game.season.tournament_leaders(results: list[Result])`** reuses `JourneyAccumulator.add_batting`/`add_bowling` on EVERY match's both innings with no `track` filter at all, then `_leader()` (already keyed on `person_id`, never a name string, per this file's own standing rule -- two different drafted seasons can share a registry name). Takes a flat list rather than a `Season` object specifically so a room's own fixtures (never wrapped in one, A79) can call it identically: solo passes `season.results + season.playoffs`, a room passes `[e.result for e in replay.results]`. Both API surfaces gain `orange_cap`/`orange_cap_runs`/`purple_cap`/`purple_cap_wickets`, computed once solo's season is complete and progressively for a room (cheap, and correct the same way `table` is already progressive) rather than gated on `replay.complete`. Displayed as two small accent-bordered cards -- literal orange and purple, since those are the award's own real colours and cricket fans read them as such on sight -- directly below the standings table in both the solo result screen and a room's league-complete screen, via one shared `capRowHtml()`. **Verified live against a running server for both**: a completed 10-seat room showed "Abhishek Sharma, 940 runs" / "Harmeet Singh, 47 wickets" in the previously-empty space; a completed solo season (74 total fixtures across 19 innings pairs) showed "SS Iyer, 1168 runs" / "S Gopal, 55 wickets" via the direct API and confirmed again through the real "Play the season" button. Full suite unaffected (407 passed, 1 skipped; 21/21 validation) -- no migration, no ETL/ratings table touched, a pure read over data the engine already produced. |
| A89 | **A squad-review screen sits between the draft finishing and the first ball, in both solo and a room, and a room's host is the one who unlocks the season past it -- confirmed directly with the user.** Neither context had one: solo's finished-draft screen already listed all twelve cards but kept ratings gated by `draft_mode` right up to and past the "Play the season" click, and a room had NO gate at all -- `replay_room_matches` ran straight from a completed draft into the first toss, so nobody ever saw their own finished twelve before matches began. **Three new team-level numbers, declared rather than measured** (same category as `REPUTATION`/`ALLROUNDER_RUNS`, A57/A59): `etl.feasibility.team_rating()` takes the twelve drafted `Card`s and returns `overall` (plain mean of `display` across all twelve), `batting`/`bowling` (means restricted by `role`, an all-rounder's one blended rating counted toward BOTH deliberately -- there is no batting-only/bowling-only number per card to split, since `display` is already batting+bowling summed, A55/A58/A60). `None`, never a fabricated 0, when a bucket has nobody in it (A33/A43's own rule). One function, four call sites, each already holding the right "11 order + Impact" list: solo's `SessionOut` (gated on `s.squad_complete`), a room seat's `RoomPlayerOut` (gated on `seat.done`, unconditionally true for a filler), and -- **overall only**, per the user's own ask -- solo's `SeasonProgressOut` and a room's `RoomMatchOut`, both carried into the journey card's now-four-tile `drawJourneyCard` (`[d.overall_rating, 'RATING']` alongside runs/wickets/matches). **Ratings are shown on this screen unconditionally, even in 'memory' draft_mode** -- `ratingBadge(card, forceReveal)` gained a second parameter that bypasses the mode gate entirely, since this is a reveal rather than a leak (the data was always sitting in the browser's own JSON either way); every existing call site keeps the old behaviour by simply omitting it. **The room half is a real synchronisation point, mirroring the existing `_advance_recorded`/`advance_match` pattern exactly rather than inventing a new kind of gate**: `replay_room_matches` returns `RoomMatchReplay(awaiting_start=True)` and resolves nothing at all -- not even a first toss -- until a `{"kind": "start"}` move appears in `room.match_moves`, recorded by the new host-only, idempotent-on-repeat `start_matches()` (same convergence philosophy as `rooms.play_again`/`advance_league_reveal`: a double-click is a no-op, not an error) via the new `POST /api/rooms/{code}/match/start`. Every seat reviews their OWN squad off `RoomPlayerOut` (already refreshed every poll regardless of room status, needing no new field), reusing the existing `roomOrderRow` -- extended with the same `forceReveal` parameter -- rather than inventing a new render function, so the row markup (icon, keeper badge, name, rating badge) stays in exactly one place; only the host sees "Continue" (`RoomMatchOut.you_decide_start`), everyone else sees "Waiting for the host to continue…", the identical `advance_ready`/`you_decide_advance` shape already used for round advancement. **Verified falsifiable**: `tests/test_feasibility.py` (new file) pins `team_rating()`'s allrounder double-count and empty-bucket-is-None rules, both confirmed to fail with either rule broken; `tests/test_room_match.py` gained four tests for the start gate, confirmed to fail with the `if not _start_recorded` check removed (a `RoomError` bubbles up from inside `start_matches` because `awaiting_start` incorrectly stays `False`). Existing room-match test helpers (`_complete_final_room` and its cup/league twins) needed one added line each -- call `start_matches` once the draft completes -- otherwise every pre-existing room-match test broke on the new gate, which is itself evidence the gate is real rather than decorative. Full suite 407 -> 417 (1 skipped unchanged), 21/21 validation unaffected -- no migration, no ETL/ratings table touched, application code only. |

| A90 | **The over-by-over reveal stopped throwing away the first innings the moment it started chasing, and gained its first CSS animation, confirmed directly with the user as "barely understandable" and a request to make it "more animated or cooler."** `startOverStepper`/`renderOverStep` (`index.html`, shared verbatim between solo and rooms) painted the live innings as three plain text swaps and showed the first innings' score exactly once, at the innings break, then discarded it -- the second-innings reveal that followed had no target, no required rate, nothing to compare the climbing score against. **Needed no backend change**: `InningsOut` (`web/app.py:230-243`) already ships a finished innings' complete `batting`/`bowling` arrays, and "home always bats first" is enforced in `game/season.py` for both the solo and room code paths, so the prior innings and its label are always knowable client-side from data already in hand. `startOverStepper` gained a 4th parameter, `priorContext: {innings, battingLabel} | null` (null only for a match's first-ever innings), painted once by the new `renderOverPrior()` into a persistent recap card above the live score -- final score, top batter (`inningsTopBatter`, filtered on `faced_any` so a did-not-bat 0-off-0 can't beat a real duck on the runs tie-break), top bowler (`inningsTopBowler`, tie-broken on economy) -- plus a live `renderChaseLine()` recomputed every tick, handling three end-states explicitly rather than letting the target line just vanish when the log runs out: still chasing ("Need X off Y, RRR Z"), target reached ("Target chased down"), or the innings ended short ("Fell short by N") -- the last one keyed off `isFinal`, since `over_log`'s own docstring says the partial final over is never logged, so `i >= log.length` is exactly the moment a chase actually resolves and the only point `ballsLeft > 0` can still be lying about balls remaining. Confirmed during design review that every skip path (`skipOverStepper`, `revealSkipThisMatch`, `revealSkipToEnd`, `revealSkipGroupStage`, `roomSkipAhead`) either re-renders through the same `renderOverStep` for free or bypasses `#overStepper` entirely onto a screen that already shows both final scores side by side -- none needed touching. **First `@keyframes` in the file** (a score pop, a red wicket flash, a per-over "chip" strip built from `over_log` as a cheap worm-chart substitute with no charting library, and the pre-existing `.gauge` fill-bar reused verbatim for an overs-progress bar rather than inventing new CSS) -- re-triggering one on a repeat tick needs the element reflowed between removing and re-adding its class (`el.classList.remove(...); void el.offsetWidth; el.classList.add(...)`), not just toggled, called out explicitly since nothing in this file had exercised that idiom before. The match-result headline (`revealCompletedMatch`) reuses `rollDeal`'s own decelerating-flicker technique (and its `ROLL_TIMERS` map) rather than new code, because it's a genuine "not known until this instant" reveal -- unlike the toss screen, which only ever shows the winner a choice they've already earned, confirmed by re-reading its own "only ever shown when you actually won it" comment before reaching for the same flicker there and rejecting it as decorative noise over nothing. No migration, no Python touched, no new backend tests -- verification is live-browser, matching every other pure-frontend change in this session. |
| A91 | **The room draft's per-pick countdown stopped visibly skipping by 2 and gained a shrinking, colour-shifting gauge bar, confirmed directly with the user as "flaky."** `#roomTimer` was written straight from each poll's `seconds_remaining` (`ROOM_POLL`, `setInterval(pollRoom, 2000)`), so the number only ever changed once every 2 seconds, dropping by 2 at a time rather than counting 1-by-1. **Fixed by separating the DISPLAY clock from the POLL clock rather than polling faster** (which would only have halved the same artifact, not removed it). `pollRoom` now stores the server's own reading as `ROOM_TIMER_BASE = {value, max, at: performance.now()}` on every poll -- the one place a genuinely fresh authoritative value arrives -- and a new `ROOM_TIMER_TICK` (`setInterval(tickRoomTimer, 1000)`) renders `value - elapsed` once a second in between, extrapolated from real wall-clock time rather than decremented by a fixed step, so a delayed or throttled tick still shows the mathematically correct remaining time instead of drifting. **The resync deliberately lives in `pollRoom`, not in `renderRoomDraft`**: `renderRoomDraft` also re-runs off the same cached `ROOM` object from `toggleRoomView` (a plain UI toggle, no new server data), and resetting the base there would have snapped the countdown backward on every such re-render. Visual upgrade reuses the existing `.gauge` bar pattern (already used for pick progress) rather than inventing new chrome: a bar beneath the number shrinks with the fraction of time left, and both the number and bar switch to a pulsing red `urgent` state inside the final 5 seconds. **Verified live against a running server**: successive readings confirmed the displayed value and bar width always track true elapsed time regardless of the tick's own firing cadence, and the urgent styling toggles correctly at the 5-second boundary and clears above it. No migration, no Python touched, application code only. |
| A92 | **A room draft could freeze on a pick because the plain read-only 2-second poll took the SAME write lock a pick submission needs, and nothing anywhere had a timeout -- diagnosed from the user's own report of drafts freezing "sometimes."** `_load_room` always issued `SELECT ... FOR UPDATE`, and `room_state` (the poll target, `GET /api/rooms/{code}`, hit every 2 seconds by EVERY connected seat) always took it and wrote back even when nothing had changed -- so with two or more seats polling, requests against the same room routinely queued behind each other, and a pick landing behind a slow poll (a Neon compute waking from the free tier's idle suspend, a cold Vercel Lambda re-running the ~4s deck load) waited with it. Nothing bounded that wait: no `lock_timeout` on the Postgres side, no `connect_timeout` on the connection, and `web/static/index.html`'s `api()` did a bare `await fetch()` with no client-side timeout either -- a stuck request just sat, and `busyClick`'s own busy-wait dimming is indistinguishable from a genuine freeze to whoever's looking at it. **Three fixes, none of them a workaround for the others.** (1) `_load_room` gains a `lock` parameter; `room_state` now does a LOCK-FREE read first and only escalates to the locked read-resolve-save when the pending turn's clock has actually run out -- provably equivalent to "would `_resolve` do anything at all," since every WRITE path (`submit_pick`) already resolves before saving, so the stored `status`/`moves` can only go stale by time passing with nobody writing. (2) `_room_db` sets `connect_timeout=10` and `lock_timeout = '5s'`, paired with two new FastAPI handlers (`psycopg.errors.LockNotAvailable`, `psycopg.OperationalError`) that turn a genuinely stuck lock or connection into a clean, retryable 503 instead of hanging until the hosting platform's own timeout eventually kills it. (3) `api()` gains a 15-second `AbortController` timeout, so a request that stalls in flight for any OTHER reason still fails visibly through the existing `slip()`/`busyClick` reset rather than leaving the panel frozen with no way out. **Verified live against the real database, not just unit-tested**: with a second raw connection holding the room row's exclusive lock open, `GET /api/rooms/{code}` returned in 3.3s (unaffected, proving the poll no longer waits on it) while `POST /start` (a genuine write) took 7.5s and returned the clean 503 message rather than hanging, and normal operation resumed within one request of the lock being released. `tests/test_rooms.py` gained `test_load_room_lock_false_omits_the_row_lock`, checked against the actual SQL text sent (not just the return value) and verified to fail when the lock-skip is reverted; full suite 417 -> 418, 21/21 validation checks unaffected (no ETL/ratings table touched, no migration, application code only). |

| A93 | **A room's semi-final (or Qualifier/Eliminator) could silently re-simulate into a DIFFERENT scoreline the moment its sibling fixture's own toss got answered, and only when both sides were real players -- diagnosed from the user's own report that a bot-involved match stayed correct while a player-v-player one didn't.** `_Driver` gave every fixture in a round ONE SHARED, continuous `random.Random(room.seed)` stream, drawn in list order (`_resolve_round`'s own iteration: Semi-final 1 before Semi-final 2, Qualifier 1 before the Eliminator, etc.). `resolve_auto` (the round-robin, bot-style) never pauses, so it always consumes the identical amount of stream on every replay -- safe. `resolve_fixture` (every knockout fixture, since ANY of them can involve a real toss) is NOT safe: while its own toss is unanswered it draws exactly ONE value (the coin flip) then raises `_OpenMatchNeedsToss`; the INSTANT that toss is answered, the SAME call instead draws the coin flip PLUS the ENTIRE REST OF THE MATCH -- thousands more values. Nothing else in the round knows this happened, so every fixture LISTED AFTER it starts drawing from a completely different point in the stream than it did a moment ago, re-simulating an ALREADY-RESOLVED-AND-SHOWN sibling into a new result out from under whoever already saw the first one. Reproduced exactly: a cup's Semi-final 2 (both real seats) answered and resolved to a genuine 258-run innings while Semi-final 1 sat pending; the instant Semi-final 1's own toss was ALSO answered, replaying showed Semi-final 2 at 248 runs -- same seed, same moves, different match, and even home/away had swapped sides. This is precisely the gap this module's own docstring already claimed was closed ("attempted independently every replay pass") but the shared stream never actually delivered. **Fixed by giving `resolve_fixture` its own independent stream per named fixture** -- `random.Random(f"{room.seed}:{stage}")` instead of `self.rng` -- so a fixture's outcome depends only on its own room seed, its own stage name, and its own recorded moves, never on a sibling's resolution timing. `resolve_auto`/`self.rng` (the round-robin and its cache, A57) are untouched, since they were never exposed to this: none of their 70 fixtures can ever pause. `str` seeds, not a bare tuple -- `random.Random` hashes a string deterministically regardless of `PYTHONHASHSEED`, which a raw tuple does not guarantee. **The existing parallel-fixtures test came one assertion short of catching this itself**: `test_two_fixtures_in_the_same_round_resolve_independently_in_either_order` already answered Semi-final 2 before Semi-final 1 and checked Semi-final 2 turned non-null, but never re-checked its VALUE after Semi-final 1 also resolved -- extended to do exactly that, verified to fail on the unfixed code (248 vs 258, the literal live-reproduced numbers) and pass on the fixed code. The comparison is by PID, not `Side` identity (`_winner_pid` maps through each call's own `home_pid`/`away_pid`) -- comparing `.result.winner is` across two different `replay_room_matches` calls hits the exact `Side`-has-no-`eq=False` trap this codebase's own comments already warn about elsewhere. **Verified live against the real database, not only unit-tested**: a real 4-seat cup room, Semi-final 2 answered first and resolved to 169/6 v 160/8 (P), Semi-final 1 then answered and resolved to 143/7 v 132/3 (P) -- Semi-final 2 re-polled twice afterwards and read back 169/6 v 160/8 (P) both times, unchanged. Full suite 418 (no new test function, an existing one extended), 21/21 validation unaffected -- no migration, no ETL/ratings table touched, application code only. |

**A26 is not settled.** The thresholds classify twelve undisputed players correctly, and
twelve is a small anchor set. **When §7 lands and the top-20-per-cohort lists print, read
them for a player who looks miscategorised** — the shape to watch for is a pure batter
tagged all-rounder on the strength of a few tidy overs. If that appears, the thresholds
get retuned then, against the larger evidence. Do not touch them before then, and do not
treat the current values as ratified beyond the twelve.

**And one extra question to ask in that same pass, filed from A40:** the pre-merge
`finisher` pool was dominated by all-rounders — Warne, Ashwin, Cummins and Jadeja sat in it
beside Dhoni, Karthik, Russell and Bravo. **Does retuning the all-rounder threshold change
5-8 batting coverage?** A26 decides who is an all-rounder, and that decision is what pushes
a player out of a batting band into a bowling one, so the two are coupled and only one of
them has been measured. The merge makes this non-urgent — it does not answer it.

## Open questions

- **`wickets_down` on `deliveries`** — deliberately not added. Derivable with a window
  function; revisit as a migration when §7.1 lands if the recompute cost is annoying.
- ~~**Fielders on `deliveries`, as the fallback keeper signal.**~~ **Closed 2026-07-30,
  A30 — the idea was tested and does not work.** It was filed as the better keeper
  signal, needing a migration and a full reload. It needed neither: keeper derivation
  already reads the archive, so catches were measured for free. Cricsheet has **no keeper
  flag** on `fielders[]` — only `substitute` — so a catch behind the stumps cannot be
  told from one at slip, and the top non-bowling catch-taker is the proven keeper only
  51% of the time. It survives as a *ranking* in `keepers_by_season.csv`, nothing more.
  **Do not reopen this as a way to avoid the hand-fill; it was already tried.**

## Stage status

| Stage | State |
|---|---|
| 1. Scaffold + migrations + runner | done |
| 2. Downloader | done; 5 archives fetched, manifest + checksums written |
| 3. Franchise reconnaissance | done; mapping confirmed, 15 franchises / 166 franchise-seasons |
| 4a. Parser (pure, no DB) | done; 1,243 matches parse, 295,732 deliveries, 16 tests pass |
| 4b. Migrations applied | done; 4 applied to Neon, 8 tables / 14 checks / 21 indexes, runner idempotent |
| 4c. Loader, 2016 only | done; 60 matches, 14,096 deliveries, 1,324 appearances, 160 people, 8 franchise-seasons |
| 4d. Full archive loaded | done; 1,243 matches, 295,732 deliveries, 28,106 appearances, 816 people, 166 franchise-seasons, 70 MB, 33s |
| 5. Validation 1-7 | done; check 5 now live off `squad_members`, check 6 now live off the state model |
| 5b. Checks 15 and 16 | done 2026-07-30; both had been asserted in SPEC as measured fact with no standing check behind them. 15 regenerates all 295,732 scorecard references from `legal_ball`; 16 pins the unknown-length innings at exactly 6. |
| 6. Squads, roles and bands | **done 2026-07-31.** Check 19 PASSES: **166 of 166** franchise-seasons offer a keeper (139 with one, 27 with two). The last 26 were resolved from the public record, not from the archive — see A52. |
| **7.1 State model** | **done 2026-07-30**, migration 007 applied. 146,159 balls faced into 80 states (5 never observed, kept as zeroes) plus 161 exact-wicket runs-remaining states, 88 kB. Checks 6, 8 and 20 written; check 6 came off SKIP. **A31 corrected SPEC's wicket-cost rule — the old wording priced a wicket by differencing runs remaining, which is confounded and goes negative.** |
| **7.2-7.3 Shrinkage + gates** | **done 2026-07-30**, migration 009 applied. State model refit striker-only (7,139 of 7,458 dismissals), **5,149 `player_season_impact` rows** written, `player_season_rating` view live. Ratified: two gates on the scoring-set denominator (A33), striker-only attribution (A36), one fallback rule (A37), long grain (A38). Provisional and paired: A34 residual + A35 `k`, both post-§7.4. `tests/test_impact.py` pins A37 and the gate arithmetic, 15 tests. |
| **1.1 Draft feasibility** | **done 2026-07-30**, out of order and deliberately so. §7.4 normalises toward the slot template, so normalising before knowing the template is legal would be wasted work. `etl.feasibility` runs the draft; check 12 asserts it and was verified falsifiable. **Template retuned and SETTLED (A40 closed):** `middle` + `finisher` merged into `middle_or_finisher` x3, keeper resolved by the CSV. Rational guarantee-off failure 1.7% -> **0 of 2,000**, the guarantee is a net again, and `opener`/`top_order` are unmoved so the bottleneck did not shift upward. **[The slot template this row describes is retired — A72/A73, see the row below.]** |
| **7.4-7.6 Normalisation + rating scale** | **done 2026-07-30**, migration 010 applied. **A view replacement and nothing else — no table, no column, no stored number.** Within-season centring (unweighted, no SD division), pooled cohort offsets zeroed below 20 observations, one global SD, linear 0-100 clipped at S = 3.5. **1,812 rateable rows; scale min 6.7, max 100.0, mean 50.0, SD 14.3.** A34/A35 resolved in the same pass (A46): band prior, k = 100, `etl.impact --write` re-run, all 5,149 rows `prior_source = 'band'`, Kohli 19 of 19 still reconcile. `etl.feasibility` now ranks on `normalised_per_ball`. A9 closed. |
| **7.8-7.12 Card, reputation floor, rate everyone** | **done 2026-08-01**, migrations 011-016, view replacements throughout. Per-match rebasing (A54), disciplines added (A55), Player of the Match priced in (A56), a continuous all-rounder term (A59), a 70-99 integer scale (A58/A60). **A33's gate stopped removing a card and started only recording why a season is thin** (A65): a thin season is rated from its prior instead of dropped, shrunk on MATCHES for the per-match quantity (A66), with the career acting as a convex FLOOR rather than a blend (A67/A69). Season means and cohort offsets are estimated on gate-passing seasons and applied to everyone (A68). **A71 closed the last gap**: the 4 of 3,337 squad members with zero balls in EITHER discipline all season are rated from their own career (2 of the 4) or a shared scale floor (the other 2) — every squad member is a real, draftable `Card` today, verified against `load_deck`. Checks 6, 9, 22 all carry A71-specific assertions, each verified falsifiable. SPEC.md's §7.3/§12.5 prose and several code comments (`etl/feasibility.py`, `game/__main__.py`, `game/simulator.py`, `web/app.py`) still described the pre-A65 gate as of 2026-08-01 and were corrected in place, with the old text named rather than silently replaced. **A74 (migration 018, 2026-08-02) retuned the scale's high anchor from the 99.8th percentile down to the 99th**, after counting showed 82% of the deck reading 70-79 under the old anchor; see A74 for the measurement and the wrong turn (raising the low anchor instead) that was tried and rejected first. **A75 (2026-08-03, no migration — application code only) fixed the card's all-rounder icon**, which had silently degraded to "rated in both disciplines at all" the moment A65 (in this same stage) removed the volume floor everywhere; it now defers to the already-calibrated `role` (A26) instead of re-deriving from bare rating presence, dropping the all-rounder count from a wrong 1,816 to the correct 284. |
| **1.1/10/11 redesigned: position eligibility + a direct twelve-pick draft** | **done 2026-08-02**, migration 017, no view/table changes beyond it. **A72**: the slot template (A6/A40) is retired for career position-eligibility (`MIN_INNINGS_AT_POSITION = 5`, `person_batting_positions`, check 23). **A73**: the draft itself becomes twelve picks straight into the final twelve (`TWELVE_SIZE` replaces `SQUAD_SIZE`), placement final at pick time, no bench. `TAIL_DEFAULT` becomes `LOWER_ORDER_BAND = (7, 11)` with a second widening case for lower-order-only evidence (check 24). The forward check collapses from bipartite matching to two count checks (`could_still_complete`); `best_twelve`/`Twelve`/`_match_positions`/`_wildcard`/`can_field_twelve`/`could_still_field_twelve` deleted. `web/session.py`'s `Pick`/`Place` collapse into one `Pick(index, slot)` move; `/api/draft/{state}/place` is gone, `/pick` takes both; `/api/xi` is `/api/twelve`. A real pre-existing bug (`pick_random` reading the global `random` module instead of the seeded `rng`) was found and fixed in the same pass. Checks 12/24/25 rewritten; `tests/test_twelve.py`, `test_draft.py`, `test_web.py` updated; all 179 tests and 21 validation checks pass. **[The per-exact-position eligibility this row describes is retired — A76, see the row below.]** |
| **1.1b Career-based batting role (A76)** | **done 2026-08-03**, migration 021, no view changes. Per-exact-position eligibility (A72/A73) retired for a single career-based role — `top`/`middle`/`finisher`/`tail` — per player, each granting a fixed 3-4 slot range. New table `person_season_batting_positions` (the per-season twin of A72's table, from the same scan, 6,616 rows) feeds a season-then-career-then-thin-then-tail cascade in the new `etl/batting_roles.py`, so a player's role can swing between seasons (SP Narine: opener 2017-2020/2024-2026, tailender 2013/2014/2022/2023) without a hand-maintained outlier list. `Card.career_positions` renamed `Card.positions` (plain field); `MIN_INNINGS_AT_POSITION`/`LOWER_ORDER_BAND` deleted. Check 24 rewritten under the same number to verify the new cascade's wiring; check 6 extended to cover the new table. Draft-completion re-measured rather than assumed to still hold: `rational`/`naive` 0% failure (2,000 drafts, matching A73's target), `random` 0.5% (better than the pre-A76 0.7%). `tests/test_batting_roles.py`, 36 tests, all verified falsifiable; full suite 335 tests and 21 validation checks pass. |
| **10. The game** | **done 2026-07-30**, and it was out of scope until it was — SPEC §9 records the move rather than deleting the line. Draft, XI selection and a twenty-over-a-side match off the stored grids; no writes, no migration, nothing in `etl` imports it. Ratified: the tilt (A47), the band below-floor batting delta (A48), the known-count overseas bound on both sides of a repair (A49), the five-bowler cap (A50). `--validate` matches the archive on total, extras and wickets and **misses on SD, 27.1 vs 33.2, recorded not tuned.** `tests/test_simulator.py`, 19 tests, all verified falsifiable — three had to be rewritten because they were not. **[XI selection here means the OPPOSITION's algorithmic side only, as of A72/A73 — the human drafter's own selection is the redesigned draft below.]** **[The Impact Player decision described implicitly here (a pre-match argmax) is superseded by A78 — see the Section 12 row below and the decisions log.]** |
| **7.8-7.9 The card** | **done 2026-07-31**, migrations 011 and 012 applied. Runs per MATCH (A54), disciplines added (A55), Player of the Match priced in (A56), a declared reputation blend (A57), integer 70-100 percentile-anchored scale (A58). **A view replacement only.** `etl.feasibility` now reads `rated_per_ball`; check 22 pins the card against the engine. Then 012: a continuous all-rounder term (A59) and a 70-**99** scale (A60). Kohli 2016 75.4 -> **96**, Bumrah's range 37.6-92.9 -> **77-96**, Narine 2024 69.7 -> **99**, finishers 20% -> **0%** of the top 20. |
| **12. The season** | **done 2026-07-31.** Ten sides, fourteen matches each, a table on points and NRR, then Qualifier 1 / Eliminator / Qualifier 2 / Final. Opposition is nine real franchise-seasons (A63). The deal shows the whole squad greyed with reasons (A64), and cards carry a kind so the UI can mark bat / ball / both / gloves. `tests/test_season.py`, updated 2026-08-04 for A78, 55 tests, all falsifiable. **A77 (2026-08-04, no migration) let a career keeper satisfy a squad's keeper requirement off any season of his own, not only the ones the archive attributes the gloves to.** **A78 (2026-08-04, no migration) moved the Impact Player decision from a pre-match argmax to a break-time, single-discipline, first-innings-informed one, and added the tailender-for-a-batter substitution/reshuffle rule** — see the decisions log for both; the measured before/after bat-vs-bowl split is A78's own entry. **A81 (2026-08-04, no migration) removed the room Impact-choice override entirely — `decide_impact` runs unattended for either side.** **A82 (2026-08-04, migration 024, comment-only) replaced the room match engine's flat sequential fixture walk with round-based parallel resolution and per-format elimination timing**, both verified live against a running server. **A83 (2026-08-04, no migration, frontend-only) fixed the reveal animation silently skipping a room's own LAST fixture in every format** — `roomMyMatchToReveal` now falls back to `results` once a fixture is no longer in `current_matches`, verified live for `final`/`cup` and structurally for `league`. **A84 (2026-08-04, no migration) let a finished room be replayed in place** — any seated player's "Play again" resets the same room to its own lobby (fresh seed, cleared move logs, CPU seats dropped), idempotently, via the new `POST /api/rooms/{code}/play-again`; solo already had the equivalent for free (`newDraft` never touches the persistent client-side draft-mode global). **A85 (2026-08-04, no migration) replaced a filler seat's live fantasy draft with a real historical franchise-season squad**, reusing solo's own opposition pipeline (`game.season.historical_sides`) — a filler never occupies a turn or competes with a human for a card again, confirmed live and by a deterministic test. **A86 (2026-08-05, no migration) let a league room's 70-match group stage be revealed host-paced, one fixture at a time or skipped straight to the settled table**, layering a new host-owned reveal cursor on top of A57's already-decided round-robin cache rather than re-simulating anything — verified live against a running server (real ball-by-ball reveal animation, correct progressive table, correct skip-ahead) and by a falsifiable pacing-vs-skip-ahead outcome-equality test. **A87 (2026-08-05, no migration) replaced a room's flat, undifferentiated results list (up to 74 identical rows, champion banner buried at the bottom, every decided match badged the same green regardless of who actually won) with solo's own already-accepted split — the caller's own group-stage form plus a small universally-relevant playoffs bracket, personally win/loss-badged via a new `ResultOut.you_home` field** — verified live for both 'league' and 'cup' champions, including a correctly RED loss badge on the caller's own eliminated semi-final. **A89 (2026-08-05, no migration) added a squad-review screen between the draft finishing and the season starting, in both solo and rooms** — every drafter sees their own finished twelve with ratings always visible (even in 'memory' mode) plus three declared team-level numbers (batting/bowling/overall, `etl.feasibility.team_rating()`), the overall number also folded into the journey card's now four-tile display; a room additionally gates on a new host-only `start_matches()` move, mirroring the existing advance-gate pattern, so nothing resolves — not even a first toss — until the host clicks Continue past their own squad review. **A90 (2026-08-05, no migration) threaded the first innings' score and best performers through the second-innings reveal and gave the reveal its first CSS animation** — a persistent recap card plus a live target/required-rate line (`priorContext` on `startOverStepper`, shared by solo and rooms), a score-pop/wicket-flash/per-over-chip-strip/overs-progress-bar, and a `rollDeal`-style flicker on the match-result headline — confirmed live in the browser for both solo and a room. **A91 (2026-08-05, no migration, frontend-only) fixed the room draft's per-pick timer visibly skipping by 2 seconds and gave it a shrinking, colour-shifting gauge bar** — the display now ticks locally once a second off wall-clock elapsed time (`ROOM_TIMER_BASE`/`tickRoomTimer`), resynced to the server's own reading only on the poll that actually fetched it, not on unrelated re-renders like `toggleRoomView` — verified live against a running server. **A93 (2026-08-05, no migration) fixed a room's knockout fixtures silently re-simulating into a different result the moment a SIBLING fixture's own toss got answered** — every fixture in a round shared one continuous rng stream, and a still-pending human fixture consumed a wildly different amount of it than a resolved one, shifting every later sibling's own draws out from under it; each fixture now draws from its own stream seeded by `(room.seed, stage)`, verified live against a running 4-seat cup room and by an extended, falsifiable test. |
| **11. The API** | **done 2026-07-31.** Stateless, seed-based (A62), five routes in `web/app.py` over `web/session.py`. No storage, no accounts, no migration. `run_draft` reused verbatim rather than reimplemented. Verified end to end against the real deck: 1,689 cards, a 15-pick draft, a legal XI and a scorecard; the same state returns an identical match on repeat requests. **The HMAC in SPEC 11 was dropped while building** — replay makes the state self-validating, so a signature protected nothing. `tests/test_web.py`, 19 tests, all verified falsifiable (one had to be rewritten). **[Superseded by A72/A73: the deck is 3,337 cards today (A65/A71 rate everyone), the draft is twelve picks not fifteen, `/api/draft/{state}/place` is gone (pick and place are one move), and `/api/xi` is `/api/twelve`.]** |
| 7.7 Ratings read + checks | **not started.** The top-20-per-cohort print against normalised numbers, and **checks 9-11, 13 and 14, which §7.4 has now unblocked** — write them against the real normalised rating, not before. A26's thresholds get re-read against those lists at the same time (watch for a pure batter tagged all-rounder), together with A40's filed question about all-rounders dominating the 5-8 band. |

2016 was checked against the public record before being called done: Kohli 973 runs off
**640** balls (the all-time single-season record — recorded here as 637 until check 7
corrected it, see A22), Warner 848, de Villiers 687, Bhuvneshwar Kumar 23 wickets (Purple
Cap), SRH 17 matches. Display names came out era-correct ("Royal Challengers Bangalore",
"Delhi Daredevils", "Kings XI Punjab", "Rising Pune Supergiants"), which is what
`franchise_seasons.display_name` exists for. One parser warning, `980989 innings 1`, and
its 108 deliveries carry a null scheduled length.

The full archive produced exactly the 6 A17 warnings and no others, and every headline
figure matched what the parser had reported before any database existed.

## Validation

```bash
uv run python -m validation                  # checks 1-6, 8, 15-20
uv run python -m validation --check 20       # one check, BY ITS SPEC 8 NUMBER
uv run python -m validation --scorecards     # check 7, for reading by hand
uv run python -m validation --match 598027   # one scorecard
uv run python -m validation --leaderboards   # check 8's lists, for reading by hand
```

**21 checks: 21 pass, 0 fail, 0 skip.** Checks 6 and 13 were rewritten for A65/A68 — 6 now asserts the view offers every season that faced or bowled a ball (reverting it to A33's gate fails it), and 13 measures drift on the seasons the offsets are estimated from. The suite went fully green for the first time on
2026-07-31: check 19 came right when the last 26 keepers were filled (A52) and check 21
when the last 13 nationalities were (A51). Nothing skips: check 6 came off SKIP when
migration 007 gave it a derived table. **Checks 12, 24 and 25 were rewritten again on
2026-08-02 for A72/A73**: 12 checks `order_errors` against the new atomic construction
rather than a separate `best_twelve` solve, and reports (not asserts) the `random` policy's
small genuine stranding rate rather than treating it as a failure; 24 (renamed from "the
tail-only default") verifies all three branches of the `LOWER_ORDER_BAND` widening, not
just the zero-evidence case; 25 checks `order_errors` against `run_draft`'s own
construction directly, since `best_twelve` — the second solver it used to be checked
against — no longer exists.

**Checks 6 and 24 were rewritten again on 2026-08-03 for A76.** 24 (same number, new
mechanism) now verifies the career-based batting-role cascade's wiring — an independent
recompute from `person_batting_positions`, `person_season_batting_positions` and
`squad_members.role` reproduces every card's `positions` exactly — rather than the
retired `LOWER_ORDER_BAND` widening rule it used to pin. 6 gained a new derived table to
examine (`person_season_batting_positions`) and a new checkable fact that did not exist
before: the season-grain and career-grain position tables are two aggregations of the
same underlying scan, so their grand totals must be identical (19,111 innings, both).

**Checks 6, 9 and 22 were rewritten again on 2026-08-01 for A71 (migration 016).** 6's exact
row count now expects `playable + zero_evidence` rather than `playable` alone, and gained a
direct completeness assertion — zero squad_members left uncovered — so a bug that dropped
four rated rows while adding four different unrated ones would still be caught. 9 gained an
explicit check on the reputation-only branch (equals the player's own career merit, or the
single shared scale floor if he has none) rather than relying on its existing bound query to
skip those rows on NULL propagation, which it otherwise would have done — silently, and
without saying so. 22 special-cases the reputation-only branch's engine-integration formula,
because the general one multiplies by `balls = 0` and always integrates to zero regardless of
the card's claim. All three verified falsifiable the same way check 6 always has been: revert
to the pre-A71 view and watch the assertion that is supposed to catch it actually fail.

**Checks 9 and 13 are written (2026-07-31); 10, 11 and 14 remain.** Each of the two was
verified falsifiable, as A2's deleted tautologies require. **Check 9** recounts `balls` from
each rating's own franchise-season and asserts A57's blend stays inside `[merit,
career_merit]` — the bound is what makes the career term shrinkage rather than leakage. It
fails on **1,767 of 1,812** rows if balls come from a career aggregate, and on **1,016** if
the blend escapes its endpoints. **Check 13** tests A2's pooling assumption across three
eras split on the Impact Player rule; largest drift is **0.055 runs/ball (batting/middle)**
and every cohort sits under 3 SE. **Its material bar is currently inert** and the check
records that in the same way check 6 records its inert clause — see the note above
`DRIFT_SES`. **The three still missing are 10 (under-shrinkage), 11 (over-shrinkage) and 14
(innings skew).** They were held back so they would not become the tautologies A2 had to
delete twice — the original 10 and 11 went for being true by construction, and check 2 for
being enforced by five foreign keys — so when they are written, **each must be shown to fail
with the thing it guards broken**, exactly as 9 and 13 were.

**10 and 11 are now harder than they were, and that is worth knowing before starting.** Both
bracket the shrinkage constant, and there are now TWO shrinkage stages: the band prior at
k = 100 inside `player_season_impact` (A46) and A57's career blend at 0.45 on top. A
within-player variance test written against the shipped rating measures the pair, not `k`,
so it cannot bracket either one alone. Write them against `shrunk_per_ball` — which the view
still exposes for exactly this reason — or state which of the two is under test.

A separate era check already exists in substance and is not check 13: **season means of
`display_rating` run 78.0 to 78.9 across all nineteen seasons**, so A42's centring survives
the per-match rebasing and the career blend. That was measured by hand on 2026-07-31 and
has no standing check behind it — which is the shape of thing this file exists to flag.

**Check 12 is written and is not one of those.** It reads the §7.3 gate, which exists, and
it asks a question no count can answer — see A40. Its predecessor, "every franchise-season
has enough draftable players to fill every slot type", would have passed while the draft
was infeasible, because a drafter is served fifteen franchise-seasons one at a time and
dedupe removes a player from every other card. It runs the draft instead.

`--check` takes the SPEC 8 number, not the position in the list. It used to take the
position, which was harmless while the checks ran 1-6 and became a silent trap the moment
the suite skipped to 17: `--check 17` reported "no such check" while `--check 7` ran it.

## Section 7.1: the state model

```bash
uv run python -m etl.state_model            # fit and print the grid, no writes
uv run python -m etl.state_model --cells    # all 80 cells with their distributions
uv run python -m etl.state_model --write    # load migration 007's two tables
```

`--write` truncates and rewrites, so it is re-runnable, and it must be re-run after any
`etl.load`. Check 20 recounts the fitting set from `deliveries` and fails if the stored
totals have gone stale, so a forgotten refit is caught rather than silently believed.

**The outcome guard.** Seven columns hold the whole distribution because 0-6 covers 100% of
the archive today — 32 fives off overthrows, zero sevens. That is a measurement, not a law:
an 8 off an overthrow is legal. So `--write` **refuses to run** if any outcome has no column,
naming every offending value, and migration 007 carries a CHECK that the seven columns sum to
balls faced. Both, deliberately: the constraint protects the table, and the loader protects
against the constraint being quietly wrong about what belongs in it. Cricsheet revises recent
seasons, so a refreshed archive is exactly when both matter. `tests/test_state_model.py` pins
the guard and was verified to fail with it removed.

## Section 7.2-7.3: per-ball impact, shrinkage and the gates

```bash
uv run python -m etl.impact                 # every distribution the decisions rest on
uv run python -m etl.impact --player "V Kohli"
uv run python -m etl.impact --bowler "SL Malinga"
uv run python -m etl.impact --reconcile "V Kohli"   # A37's check, vs the scorecard
uv run python -m etl.impact --cohorts       # SPEC 7.7 top-20 per band and usage phase
uv run python -m etl.impact --gate-gap      # the 61 seasons the scoring-set denominator flips
uv run python -m etl.impact --calibrate     # A46's prior x k sweep, on NORMALISED numbers
uv run python -m etl.impact --write         # load migration 009's player_season_impact
```

Everything except `--write` is read-only: it scores every delivery in memory against the
§7.1 grids and prints raw and shrunk lists, the floor sweeps behind A33, the `k` sweep
behind A35, and the prior-breadth and era-drift tables. Re-running it is how any figure
quoted in A32-A39 gets re-checked.

`--calibrate` is the A46 evidence and it **applies §7.4's normalisation in Python before
judging anything**, because that is the whole point: the prior and `k` are choices about
spread, and the pre-normalisation baseline they used to be judged against is the thing
§7.4 removes. It prints, per (prior, k), Kohli's retained spread, the balls in the
thinnest top-five season, and the dominated-inversion rate. `normalise()` in `etl/impact.py`
mirrors migration 010 and is the one place the two could drift — if the view changes,
that function changes with it or the calibration is measuring a scale nobody ships.

`RATED_PRIOR` in `etl/impact.py` is the single definition of which prior the ratings use
(A46: `band_prior`). `print_player` and `print_prior_breadth` stay on `loso_prior`
deliberately — they are LOSO-specific diagnostics and exist to show what was given up.

`--write` truncates and rewrites `player_season_impact`, so it is re-runnable, and it must
be re-run after any `etl.load` **and after any `etl.state_model --write`** — the impact
numbers are scored against the stored grids, so a refit grid without a refit impact is a
stale rating that still looks fresh. Order: `etl.load` -> `etl.state_model --write` ->
`etl.impact --write`.

`k` is not written. It lives in the `player_season_rating` view alone (A35), so re-tuning
it is a view replacement and nothing else. The prior, by contrast, **is** written — it is
an input to the impact numbers, not a post-hoc transform — so changing `RATED_PRIOR` needs
`etl.impact --write` re-run, where changing `k` does not. Two constants, two different
refresh rules, and mixing them up leaves a stale `prior_per_ball` under a fresh view.

## Section 1.1: draft feasibility

```bash
uv run python -m etl.feasibility                    # position supply, policies, redraw rates
uv run python -m etl.feasibility --trials 5000 --seed 11
uv run python -m validation --check 12              # the same thing, as a check
```

Read-only, no writes, no arguments needed. **[A72/A73 — this section describes the current
mechanic; the slot-template narrative below it is history, kept rather than deleted per
this file's own rule.]** It plays **twelve**-pick drafts against the real deck — uniform
over the 166 franchise-seasons, dedupe on `person_id`, the §1.1 deal-time guarantee, every
pick final the moment it lands — under three drafter policies, and reports completion,
where a stranded drafter strands, and **how often the guarantee fires**.

**Read the guarantee-off column, not the guarantee-on one.** Guarantee-on is close to 100%
and will stay there until coverage collapses, because the guarantee re-draws until it finds
a servable pick; it tells you the game works, not whether the deck's coverage does.

**A73 changed what "reads as fine" means here.** With squad size and slot count now the
same number, every pick is load-bearing rather than three of fifteen being slack, so a
small, genuine stranding rate for the **adversarial `random` policy alone** is now expected
and reported rather than treated as a defect: **14 of 2,000** (seed 7), always on the very
last pick, always because the twelve needed a keeper who ALSO bowls at whatever position
was left, and none exists in the archive for that position (measured directly: 11 of 166
franchise-seasons offer an eligible keeper there, 0 offer one who bowls too). `rational` and
`naive` are **0 of 2,000** each — a policy that manages either requirement at all does not
reach the corner a policy ignoring both can.

**The historical A40/A61 template narrative, for context on how this section used to
read:** the old slot template's guarantee-off rate moved from "fine" to "the finisher slot
is carrying all the risk," then to 0 of 2,000 after the A40 merge, then A61 deliberately
moved it back off zero (**~1.5% of drafts**) by adding the overseas cap, accepted because
that is a cap binding occasionally, which is what a cap is for. None of that machinery
(`TEMPLATE`, `BAND_SLOT`, slot supply) exists any more — position eligibility (A72) and the
count-based forward check (A73) replaced it outright.

## Section 10: the game

```bash
uv run python -m game                            # draft two twelves, play a match
uv run python -m game --seed 7 --policy rational
uv run python -m game --validate --trials 3000   # league-average innings vs the archive
```

**[A73]** Each side is twelve picks straight into a fully arranged final twelve — `run_draft`
produces `order`/`impact` directly for every policy, human or automated, so there is no
separate "pick an XI" step here any more; `print_twelve` just prints what `run_draft`
already built.

Read-only. It reads the stored §7.1 grids and the `player_season_rating` view and fits
nothing, so re-run it after any `etl.impact --write` to see new numbers — but a stale grid
cannot make it print a stale rating, because it holds none.

**`--validate` reproduces the archive on mean total (169.5 vs 169.4), extras (8.5 vs 8.5)
and wickets (6.0 vs 6.1), and misses on SD: 27.1 against 33.2.** Simulated innings are ~18%
too tightly bunched, because at league average both sides and the pitch are identical every
time, so between-team and between-venue variation is absent by construction. **Recorded, not
tuned** — if a later change moves the SD, check it moved for a reason rather than because
something got fitted to this table.

**At league average the tilt is the identity, so `--validate` cannot check it at all.**
`tests/test_simulator.py` is the only thing that does: 19 tests, every one verified to fail
with its guarded line broken. Three assertions were **vacuous when that was checked** and
were rewritten rather than kept:

- the no-consecutive-overs rule never binds in a five-man attack — whoever just bowled
  always has the most balls — so the twenty-over test passed with the rule deleted, and it
  needed a separate two-bowler test where the tie-break wants the man who just bowled;
- the overseas repair's `viable` filter needed a squad where the cheapest swap by rating is
  the fifth bowler, otherwise the naive swap happens to be legal anyway;
- the clamp inside `tilt` **cannot be distinguished from the theta rail by any test**, so
  that test now claims only what it does pin, which is the bisection's search direction.

That is three of nineteen, written carefully, passing, and protecting nothing. Break the
line before believing the test.

## Section 11: the API

```bash
uv run uvicorn web.app:app --reload          # http://127.0.0.1:8000, docs at /docs
```

Stateless and seed-based (A62). Boot reads the deck and the state model once — about 4
seconds — and **a request touches the database never**. No session store, no accounts, no
table.

| route | |
|---|---|
| `GET /api/health` | deck size |
| `POST /api/draft?seed=` | start; omit the seed for a fresh one |
| `GET /api/draft/{state}` | current deal, or the finished twelve |
| `POST /api/draft/{state}/pick` | `{"index": n, "slot": s}` — **[A73] one move, not two: `slot` is 1-11 or 12 (Impact), final the instant it lands** |
| `GET /api/twelve/{state}` | the arranged twelve, and its overseas status — **[A73] replaces `/api/xi`; no alias, no bench field any more** |
| `GET /api/season/{state}` | fourteen league matches, the table, the playoffs (~3s) |

State is `7-3:4.0:12` — seed then `index:slot` moves, **unsigned**, and SPEC 11.2 records
why that corrects the original design: replay refuses an index the server never dealt, so
there is nothing a signature would protect. **It goes back the day a leaderboard lands**,
because a result that outlives its request can be submitted without being played.

**`web/session.py` reuses `run_draft` verbatim** — the human is injected as a policy that
replays recorded choices and raises to pause. Do not reimplement the loop here: A61's cap and
A40's guarantee live in `run_draft` and a second copy is a second place for them to drift.

**The match continues the session's LIVE rng** (player draft -> opponent -> innings), which
is the order `game.__main__` uses. Re-seeding would give the API a different match from the
CLI for the same seed with nothing to notice it; `tests/test_web.py` pins it by comparing
against exactly that mistake.

## Section 6: squads, and the four CSVs that gate the deck

```bash
uv run python -m etl.derive_people --all    # nationality, keepers, bowling style
uv run python -m etl.derive_squads          # squad_members, ~30s
```

Both are re-runnable and `derive_people` is idempotent to the byte. `derive_squads`
truncates and rewrites. Run `derive_people` before `derive_squads` — the keeper role
reads `keepers_by_season.csv`, which the first command generates.

**`derive_squads` also clears `player_season_impact`, so `etl.impact --write` MUST follow
it** (A53). The full refresh order is one chain, not two:

```
etl.load -> etl.derive_people -> etl.derive_squads -> etl.state_model --write -> etl.impact --write -> etl.career_positions --write
```

**[A72, extended by A76]** `etl.career_positions --write` refreshes `person_batting_positions`
AND `person_season_batting_positions` from `deliveries` directly (one scan, two grains kept
instead of one dropped), so it has no ordering dependency on `derive_squads`/`impact`
beyond needing a fresh `etl.load` behind it — it is placed last in the chain here only
because it is the newest step, not because anything upstream of it produces its input.

Migration 009 pointed `player_season_impact` at `squad_members`, which made the plain
truncate fail — the database being right, because a squad rebuild can move a role or a band
underneath a rating keyed to it. The dependant is **named** in the truncate rather than
reached by CASCADE, so the next table to depend on `squad_members` fails loudly here
instead of being emptied in silence.

**One column per file is the decision; the rest is evidence** (A30). `merge_override`
takes the decision column by name, carries it across untouched, and refreshes everything
else, so adding a new signal reaches rows that already exist. **Nothing else in the repo
may write a decision column.**

| CSV | Decision column | Rows awaiting a human | Blocks |
|---|---|---|---|
| `nationality.csv` | `nationality` | **0 of 327** — COMPLETE (A51) | the four-overseas rule |
| `keepers.csv` | `is_keeper` | 440 of 489 — **not blocking**, A24 superseded it | (nothing) |
| `keepers_by_season.csv` | `kept` | 438 of 633 — **not blocking**, all 166 squads covered (A52) | the keeper slot |
| `bowling_style.csv` | `bowling_style` | 479 of 479 | A8 pace/spin slots (not a legal draft) |

Blank means undecided in all four. Nothing is guessed and nothing is defaulted, so an
unfilled row shows up as a NULL or a missing keeper rather than as a wrong answer. Every
row carries a `cricinfo_id` so a reviewer identifies the player instead of searching for
one; coverage is complete and a test enforces it.

`keepers_by_season.csv` grew from 345 rows to 633 because each squad now also lists its
top three non-bowling catch-takers as candidates. **The ranking is not the answer** — for
2011 Delhi it puts Sehwag first and Ojha, who kept, second. Read `catch_rank`,
`keeper_elsewhere` and `balls_bowled` together, not `catch_rank` alone.

A typo in a decision column reads as a silent "no" rather than an error, so
`tests/test_overrides.py` pins the vocabulary each reader accepts: `kept` and `is_keeper`
take y/yes/true/1 or n/no/false/0, `bowling_style` takes pace or spin.

A **SKIP is not a pass** and the runner says so. Checks 5 and 6 targeted tables SPEC 6 and 7
build; they skipped with a reason rather than reporting green against an empty table. Check 6
was written to turn into a hard FAIL the moment a derived table appeared without the check
being written, so it could not be forgotten — **and on 2026-07-30 it did exactly that.**
Migration 007 created the state tables and check 6 failed on the next run with "a derived
stat table exists but this check has not been written". The trap worked. It is now a real
pass and the suite skips nothing.

Check 6 also reports something worth knowing: `not is_super_over` in the §7.1 fitting filter
is **carrying no weight**. A super over has `innings_no >= 3` and a null
`innings_scheduled_balls`, so `innings_no = 1` and `= 120` each exclude all 175 of them
unaided. That is A21 working as designed rather than a reason to delete the clause — it
states intent — but the check asserts each clause independently so nobody mistakes the
explicit one for the thing doing the work.

**Check 6's trap fired twice more when migration 009 landed** — once for
`player_season_impact` and again for the `player_season_rating` view — and both times the
right response was a real assertion rather than adding the name to a list. The scoring set
gets the same independent-clause treatment as the fitting set, and the view is checked by
recomputing which rows *should* be offered from the gate columns and naming any row where
the view and the arithmetic disagree. A view that simply selected everything would pass a
count-based check, which is why the check counts nothing.

Check 7 is the only check a human can disagree with, and it is the only one that has ever
found anything the others could not — see A22. Five matches, hand-verified: `335982`
(McCullum 158* off 73), `598027` (Gayle 175* off 66), `980987` (Kohli 109, de Villiers
129*), `1216517` (tied, then two super overs), `1426312` (2024 final, SRH 113 all out).

2016 was the inspection season for stage 4 because it exercises Gujarat Lions and Rising
Pune Supergiants in one go. It remains the `etl.load` default. The full archive went in
before stage 5, because check 7 needs 2008/2013/2020/2024 present and one season out of
nineteen exercises almost none of the parser's traps — no super over, no `2007/08` label,
no fractional target, and one of the eight miscounted overs.
