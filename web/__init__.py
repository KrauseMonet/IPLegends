"""SPEC 11. The HTTP layer: stateless, seed-based, and it stores nothing.

Depends on `etl` and `game`; neither depends on this. The pipeline stays exactly as
testable as it was, which is the same one-way rule `game/__init__.py` records.
"""
